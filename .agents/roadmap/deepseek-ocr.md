# deepseek-ocr - roadmap

DeepSeek-OCR: a document-image-in, text/markdown-out model. DeepEncoder (SAM
ViT-B windowed+global tower with decomposed relative position bias -> a 16x
conv token compressor -> CLIP-L/14 with its patch embed bypassed in favor of
the compressor output) feeds a DeepSeek-V2-family MoE decoder (12 layers, 64
routed experts top-6 + 2 unweighted shared experts, plain MHA -- NOT MLA).

Real-weight goldens: `testdata/deepseek-ocr/real/{vision,decoder}.safetensors`
+ `manifest-real.json`, regenerated by
`tools/goldens/deepseek_ocr_convert_llamacpp_dump.py --sam-dump <dir>
--decoder-dump <dir>` from llama.cpp graph-eval dumps (that script's header is
the reproduction recipe, including the `common/debug.cpp` patch needed to make
the callback write FULL rather than truncated tensor data). `testdata/` is
gitignored, so these are re-derived, never committed.

Checkpoint: `ggml-org/DeepSeek-OCR-GGUF`, Q8_0 pair, in the model store at
`ggml-org/DeepSeek-OCR-GGUF/{DeepSeek-OCR-Q8_0.gguf,mmproj-DeepSeek-OCR-Q8_0.gguf}`.
Loader: `crates/gguf` (new shared GGUF import layer, architecture-dispatched
off `general.architecture` / `clip.projector_type`), with dedicated
`deepseek_ocr` (LM) and `deepseek_ocr_vision` (mmproj) modules. Both files
import with full two-way tensor coverage (verified by test).

## Facts pinned from the real GGUF headers (not assumed)

LM (`deepseek2-ocr` architecture):
- 12 layers, embed 1280, 10 heads = 10 kv heads (head_dim 128) -- **plain MHA,
  not MLA** (no `attn_q_norm`/`attn_k_norm`/MLA-shaped tensors present;
  matches the official HF config's `use_mla: false`).
- vocab 129280, ctx 8192, RMSNorm eps ~1e-6.
- MoE: `leading_dense_block_count=1` (layer 0 dense, ff 6848), layers 1-11
  MoE with 64 routed experts (ff 896) top-6, 2 shared experts **fused into one
  1792-wide SwiGLU MLP with no shared-expert gate tensor** -- they are summed
  unweighted, so the fused width IS the sum, not two separate 896-wide MLPs to
  reassemble at import time.
- `expert_group_count=1`, `expert_group_used_count=1` (no group-limited
  routing).
- **No `scoring_func`/`topk_method`/`norm_topk_prob`/`routed_scaling_factor`
  keys exist in this file's KV at all.** llama.cpp's compiled-in default for
  this arch is what actually runs: plain softmax router, top-6, no
  renormalization, scale 1.0, no group masking. This is not a reading of the
  HF config defaults, it's a fact about what the GGUF's own consumer (llama.cpp)
  does with an absent key -- confirmed via the merged PR, not guessed.
- `rope.dimension_count = 0`: not a placeholder -- llama.cpp never reads
  `n_rot` for this arch's attention branch. It ropes the full `head_dim=128`
  in **NEOX** (half-split) layout and asserts `freq_base==10000` (the file
  carries no `rope.freq_base` override, so the library default applies).
- `output.weight` present separately from `token_embd.weight` -> untied
  lm_head, as expected.

Vision (`clip` architecture, `clip.projector_type=deepseekocr`, the mmproj
file, 476 tensors):
- SAM tower: 12 blocks, embed 768, 12 heads, window 14. Global (unwindowed)
  layer indices `[2,5,8,11]`, derived from the rel-pos table extents in the
  real tensor shapes (not from a KV key -- none exists for this).
- CLIP tower: 24 blocks, embed 1024, 16 heads, patch 14, image_size 224 (the
  encoder's *native* size before DeepSeek's patch-embed bypass; the real
  input token grid comes from the compressor output, not this field).
- `clip.vision.feed_forward_length = 64` is a **converter bug** in the file
  itself (`heads*4` computed instead of `width*4`) -- llama.cpp only logs this
  field, never dispatches on it, so it's inert. Real FF widths (4096 CLIP,
  3072 SAM) come from actual tensor shapes. Left as-is upstream; our config
  extraction derives from tensor shapes, not this key.
- llama.cpp's own tensor names for this tower (`v.sam.blk.N.*`,
  `v.sam.{neck,net_2,net_3,patch_embd,pos_embd}`, `v.blk.N.*`,
  `v.{class_embd,patch_embd,position_embd,pre_ln,image_newline,view_seperator}`,
  `mm.model.fc`) are the ones the mmproj loader classifies against -- these
  differ from the HF checkpoint's `sam_model.*`/`vision_model.*`/`projector.*`
  prefixes, which are irrelevant here since we are not importing safetensors.

## Questions that were open, and how each was CLOSED (Phase 4 capstone)

- **Vision-concat order: CLOSED, `[clip_spatial, compressor_flat]`.** Pinned
  twice independently: the golden dumper builds it that way and asserts the
  halves by slicing them back out (`findings.vision_concat`), and llama.cpp's
  own consumer of this GGUF does `ggml_concat(ctx0, clip_out, sam_out, 0)`
  (`tools/mtmd/models/deepseekocr.cpp`), concatenating along the row width with
  CLIP first. `crates/deepseekocr/tests/tiny_ref.rs` asserts each half against
  its own source tap at widths 14 and 11; mutation-verified (swapping the two
  drops `vision_concat` to cosine 0.064 and trips the explicit half assertion).
- **CLIP `pre_ln`: CLOSED, and it was a real fixture defect.** The tower applies
  ONE LayerNorm to `[cls; tokens] + pos` before block 0 (`v.pre_ln`) and has no
  post-LN -- it is not a `norm1`/`norm2` pairing question at all. The original
  golden had no `pre_norm` and therefore did not model the real checkpoint;
  the regenerated fixture carries `clip.pre_norm.{weight,bias}`.
- **Row-layout token count: CLOSED from the reference consumer**, and the
  earlier sketch in this file was WRONG in two details. llama.cpp's
  `clip_n_output_tokens` / `clip_graph_deepseekocr::build` give:
  * global (overview) view: `h*(w+1) + 1` rows -- one `image_newline` after each
    of the `h` token rows, then ONE `view_seperator` terminating the view;
  * local tiles: `(g*tiles_w + 1) * g` rows per **tile row**, with the tiles of
    one tile-row **interleaved by token row** (tile 0 row y, tile 1 row y, ...,
    then one newline) and **NO separator** anywhere in the local branch.
    "The same as the global view, per tile" was the wrong reading.
  Implemented and unit-tested in `crates/deepseekocr/src/rows.rs`. The one thing
  still unverified is the ORDER of the two blocks (global-then-local as built),
  because the preprocessor that orders the views is outside the graph builder;
  it is flagged in that module's header and changes no row count, so only a real
  forward can settle it.

## New facts pinned by the capstone

- The CLIP position table lives on the checkpoint's **native square** grid and
  is bicubically resampled (torch `align_corners=False`, `a = -0.75`) onto the
  compressor's output grid, class-token row lifted out and re-inserted at index
  0. At real scale both grids are 16x16, so the resample is the identity and a
  bug there is invisible; the fixture runs 3x3 -> 4x2 (height up, width down).
- The composite is **batch 1**: `sam1`'s windowed spans' storage-binding offsets
  are not 256 B aligned across a batch stride. The golden fixture was
  regenerated at `batch = 1` to match.
- The golden's `sam_embed = 10` was **not dispatchable** (`16 | d_model` is
  forced by a 12-row window at 64-float binding alignment); the fixture now uses
  16 = 2 heads x head_dim 8, and asserts the rule in-script so a future
  regeneration cannot reintroduce it.

## New facts pinned by the real-weight phase (Phase 5)

Everything here is read off the shipped checkpoints or measured against
llama.cpp running them - not inferred from a config or a reimplementation.

- **The raw (un-renormalized) router gate is now a real-weight fact, not a
  fixture claim.** In the reference's own dump, `ffn_moe_probs` sums to 1.0
  across all 64 routed experts (plain softmax) while `ffn_moe_weights` - the
  values the layer actually multiplies its expert outputs by - sums to between
  **0.222 and 0.817** across the 6 selected experts, and equals the
  corresponding `ffn_moe_probs` entries **exactly** (max_abs 0). That is
  `norm_topk_prob = false` AND `routed_scaling_factor = 1.0`, observed rather
  than argued from an absent GGUF key.
  `deepseekv2::tests::real_weight_router_gates_are_raw_not_renormalized`
  asserts both halves on the reference and on brain's own gate.
- **`clip.use_gelu = true` in the shipped mmproj.** The earlier note that this
  file sets it *false* (and therefore selects quick-GELU) was WRONG, and
  `ClipVisionConfig::deepseek_ocr()` carried `QuickGelu` because of it. Fixed to
  `GeluErf`, and `crates/clip/tests/deepseek_ocr_real.rs` now asserts the preset
  against the real file so it cannot drift again. **The authority question is
  still open** and is flagged in that constructor's doc: the original CLIP-L
  uses quick-GELU, llama.cpp reads `true` as its tanh `FFN_GELU`, and brain
  follows the file's flag into exact-erf - three candidates, and no
  CLIP-internal reference tap exists to settle it (see the capture note below).
- **The CLIP tower's LayerNorm epsilon is NOT the file's key.** The mmproj
  carries `clip.vision.attention.layer_norm_epsilon = 1e-6`, but llama.cpp
  reads that into `hparams.eps` and then overwrites it with **1e-5** in its
  `PROJECTOR_TYPE_DEEPSEEKOCR` branch ("the CLIP/ViT body runs its layernorms at
  1e-5, the SAM stage uses 1e-6"); the SAM tower's 1e-6 is likewise a hardcoded
  constant in its graph builder, not a read of this key. `from_gguf` adopted the
  key verbatim and would have run CLIP at the SAM epsilon. Now overridden once,
  in `ClipVisionConfig::from_gguf`, behind the named constant
  `clip::config::DEEPSEEK_OCR_CLIP_EPS`.
- **SAM's MLP activation is exact-erf GELU, confirmed on real weights.** An
  independent longhand NumPy fp64 reimplementation of SAM block 0 (built from
  the dequantized mmproj tensors, sharing no code with brain) scores **0.99997**
  against the reference tap with erf-GELU and **0.9797** with quick-GELU. Worth
  pinning because llama.cpp derives BOTH towers' activation from the single
  `use_gelu` flag.
- **The reference is not an fp32 oracle, and the floor reflects that.** brain
  dequantizes Q8_0 to fp32 and runs fp32 GEMMs; ggml keeps the weights
  quantized and quantizes the **activations** to 8 bits per 32-element block for
  the dot product. That is a one-sided ~1e-2-relative error per matmul, so 1.0
  is not reachable and a 0.9999 floor would be wrong on the physics. The gate
  sits at 0.999 against a measured worst of 0.99944 (vision) / 0.99957
  (decoder). The same NumPy reimplementation lands at the *same* 0.9999663 that
  brain does on `sam_blk00_out` to seven digits - two independent
  implementations agreeing with each other more closely than either agrees with
  the reference is what a reference-side numeric difference looks like.
- **Tensor layouts in the reference dump, verified from the bytes.** ggml's `ne`
  is fastest-axis-first, but which axis carries what DIFFERS within the same
  graph: `sam_layer_out-N` is `(H, W, C)` with channel FASTEST (transformer-row
  form), while `sam_output` is `(C, H, W)` NCHW with channel SLOWEST (conv-graph
  form). Checked by spatial-correlation structure (0.997 adjacent vs 0.71
  opposite corners under the correct reading; 0.27 under the wrong one), not by
  reading the code.
- **The SAM -> CLIP handoff is a bit-exact identity.** Rows 1..=256 of the
  reference's assembled CLIP input are `sam_output` reinterpreted NCHW -> NLC,
  max abs difference **exactly 0.0** - so the assembly is
  `concat(class_embd, flatten_nlc(compressor_out))` with NO position embedding
  and NO `pre_ln` applied yet (those live inside the CLIP graph, after the
  concat). Row 0 is `v.class_embd` **verbatim**, which makes it a free exact
  check on the CLIP import and is asserted in two crates.
- **The `-p encode` debug input bypasses preprocessing entirely.**
  `mtmd_debug_encode_image` copies the constant `0.5f` buffer straight into a
  `clip_image_f32` and calls `clip_image_encode` - no `clip_image_preprocess`,
  no mean/std. So the network sees 0.5 everywhere, which is NOT what
  DeepSeek-OCR's real preprocessing produces for a gray image (mean=std=0.5
  would map it to 0.0). A consumer must feed the constant unnormalized.
- **No CLIP-internal or image+decoder reference taps exist, and that is
  upstream's bug, not ours.** llama.cpp's graph-eval debug callback segfaults
  deterministically right after the cls-token concat, before `build_vit`
  begins - reproducibly, with or without the full-dump patch, at multiple image
  sizes. Its production path (`llama-mtmd-cli`) runs the same graph to
  completion. So CLIP's blocks and the composed image->logits path are gated on
  finiteness and range only, with no invented cosine floor.
- **The wgpu backend corrupts `sam1` at production shape, and the parity tests
  pin the CPU backend because of it.** At 1024x1024 the tower's per-block
  buffers go wrong as soon as the graph holds **three or more** blocks:
  `block_out(0)` moves by up to 2.1e2 purely by adding a third block, and the
  patch-embed tap stops being spatially constant for a spatially constant image
  (deviation 6.66, *identical* for every block count from 3 to 12). The CPU
  backend is bit-identical between a 2- and a 3-block build. Ruled out: weight
  values (a dense-init build reproduces it), the import (every parameter
  round-trips through the ParamStore at max abs error 0.0), and global attention
  as such (a 2-block config with one global block is clean). This is a
  device/allocation-level defect, it is NOT covered by the tiny gradcheck
  fixture, and **the SAM tower cannot presently be trusted on wgpu at production
  shape.** Fixing it is its own change.

  **Follow-up investigation (checkpoint-free reproduction, root-cause bisection,
  and a partial mitigation), still open - the CPU pin in
  `crates/cli/src/resident_deepseekocr.rs` was NOT lifted:**

  * A minimal, seconds-not-minutes checkpoint-free reproduction exists:
    `crates/sam1/tests/wgpu_block_count_corruption.rs`. Two `SamEncoder`s share
    an identical block-0/1 prefix (same `SamViTConfig::deepseek_ocr()` geometry,
    same `init_dense` seed - its sequential draw over `param_list()` gives
    blocks 0/1 the same weights regardless of `n_layers`) and differ only in
    whether a 3rd, global-attention block exists past them. On any correct
    backend `block_out(0)`/`block_out(1)`/`patch_tokens()` must be
    bit-identical between the two builds; `backend-cpu` holds this exactly
    every run, `backend-wgpu` does not.
  * **First hypothesis, bisected out as INSUFFICIENT ALONE**: a single large
    2D-tiled dispatch. A reduced-dims variant sized only to cross
    `backend_api::MAX_GROUPS_PER_DIM`'s 65 535-workgroup tiling threshold for
    one `attn_relpos_add` dispatch was clean on both backends.
  * **What DOES change the failure rate** (three independent levers, found by
    bisecting which dispatch is necessary for the corruption at REAL
    `SamViTConfig::deepseek_ocr()` dims): disabling just the
    `attn_relpos_add` dispatch (the kernel that folds the decomposed
    relative-position bias into the score slab IN PLACE,
    `scores[idx] += rel_h[..] + rel_w[..]`, dispatched 16 times per global
    block at `attn_chunk=256` against `T=4096`, every one needing the
    65 535-workgroup 2D-tile path - `heads*qn*kn = 12 582 912` threads);
    shrinking `attn_chunk` so no `attn_relpos_add` dispatch needs 2D tiling;
    and `BRAIN_GPU_CHECKED=1` (wgpu's bounds-checked shader compilation).
  * **`attn_relpos_add.wgsl` was rewritten** to block `JB=8` keys per
    invocation (`crates/model/src/vit.rs::RelPos::add_step`/`ADD_JB` dispatches
    the matching reduced width), cutting the real shape's dispatch to
    1 572 864 threads / 24 576 workgroups - clear of the tiling threshold at
    every SAM-1 config this repo defines. Gradient-checked green on both
    backends (`crates/sam1/tests/gradcheck.rs`, unchanged pass/fail), no
    "not JIT-compiled" fallback on `backend-cpu`.
  * **This change does NOT reliably fix the checkpoint-free reproduction.**
    Measured by running `wgpu_backend_block0_is_unaffected_by_a_third_block`
    five times in a row AFTER the kernel rewrite: FAIL, ok, FAIL, FAIL, ok
    (2/5 clean). Critically, every FAILING run reported the IDENTICAL delta
    (`patch_tokens` max\|Δ\| 8.202e0, `block_out(0)` 2.816e2, `block_out(1)`
    3.894e2 - the exact same numbers as the ORIGINAL, pre-fix defect) rather
    than varying noise. A binary outcome - either every tap matches exactly,
    or every tap is off by one specific, repeatable wrong value - is the
    signature of a **missing synchronization barrier** (a read observing
    stale pre-write device memory on some schedules, the correct post-write
    value on others), not of a fixed index formula. Each of the three levers
    above measurably changing the failure rate (rather than doing nothing) is
    consistent with a scheduling/timing-sensitive race, not proof any of them
    is a red herring.
  * **A separate real-weight, full-12-layer, checkpoint-free-independent
    confirmation** lives in `crates/sam1/tests/wgpu_real_weight_parity.rs`
    (loads the real mmproj, compares `backend-cpu` vs `backend-wgpu` forward
    tap for tap - no llama.cpp golden needed) and a further checkpoint-free
    variant at 5 layers
    (`wgpu_backend_block1_is_unaffected_at_five_layers`) found `block_out(1)`
    (computed by WINDOWED attention alone, before any global block runs)
    already diverging by max\|Δ\| 1.209e2 with more total blocks queued -
    possibly the same race showing a different observable under more queued
    work, possibly a second one; not distinguished yet (one non-repeated run,
    and the first race's own measured 2/5 flake rate means a single run is
    not enough evidence either way).
  * **What is still unknown**: the actual missing barrier/ordering guarantee.
    Candidates not yet individually tested: whether `backend-wgpu`'s
    lazily-accumulated single-pass flush (`WgpuBackend::submit`/`flush_inner`,
    `crates/backend-wgpu/src/lib.rs`) correctly orders EVERY read-after-write
    it claims to across dozens of large dispatches queued in one
    `queue.submit`, versus a genuine driver-level (Mesa ANV, this box's Intel
    Arc iGPU) scheduling bug specific to many large 2D-tiled dispatches to one
    buffer. All three test files above are runnable, checkpoint-free (except
    the real-weight one, which self-skips without the mmproj checkpoint), and
    fast (`cargo test -p brain-sam1 --release --offline -- --ignored` for the
    three `#[ignore]`d ones) - re-run each SEVERAL times, not once, before
    concluding anything from a single pass or fail.

## Tokenizer / prompt facts pinned from the shipped LM GGUF (Phase 7c)

Read off `DeepSeek-OCR-Q8_0.gguf`'s own `tokenizer.ggml.*` KV, and cross-checked
against `deepseek-ai/DeepSeek-OCR`'s published `tokenizer.json` and
`modeling_deepseekocr.py`. All of it is asserted by
`crates/deepseekocr/tests/prompt_real.rs`, which enumerates the file rather than
looking single strings up, so a re-quantized checkpoint that renumbers a control
token fails loudly.

- `tokenizer.ggml.model = gpt2` (byte-level BPE - `QwenBpe::from_gguf` takes it
  as-is), `tokenizer.ggml.pre = deepseek-v3`, 129 280 tokens / 127 741 merges,
  `bos = 0`, `eos = 1`, `pad = 2`, `add_bos_token = true`,
  `add_eos_token = false`, and a chat template that is the identity
  (`{% for m in messages %}{{m['content']}}{% endfor %}`) - this model is
  prompted with raw text, not ChatML.
- **There is exactly ONE image token, and the newline and view-separator rows
  use it too.** `<image>` is id **128815**, and the reference builds the whole
  block out of that single id:
  `([image_token_id] * n + [image_token_id]) * n` then `+= [image_token_id]`
  (`image_token_id = 128815` is a literal in `modeling_deepseekocr.py`, and its
  `image_newline` / `view_seperator` are `nn.Parameter`s, i.e. **vectors, not
  vocabulary**). Independently: the file's complete CONTROL/USER_DEFINED
  inventory, listed below, contains no newline/separator entry, and no token of
  ANY reserved class (CONTROL, USER_DEFINED or the trailing UNUSED block)
  contains the substring `newline`, `separator` or `seperator` - the scan is
  over the reserved classes only, because ordinary vocabulary of course carries
  the English words. So the decoder-side splice has one placeholder id and the row
  *kind* lives only in `deepseekocr::rows`.
- That same reference code confirms `rows.rs`'s arithmetic from a second,
  non-llama.cpp source: the global block is `n*(n+1) + 1` rows and the local
  block is `(n*tiles_w + 1) * (n*tiles_h)` - exactly `global_rows` and
  `tiles_h * local_row_rows`.
- **The complete non-placeholder CONTROL inventory** (30 entries; ids are
  contiguous from 128800 apart from the three at the bottom of the vocab). This
  is the list `CONTROL_TOKENS` in the test asserts, id for id:

  ```text
       0  <｜begin▁of▁sentence｜>     128811  <｜tool▁outputs▁end｜>
       1  <｜end▁of▁sentence｜>       128812  <｜tool▁output▁begin｜>
       2  <｜▁pad▁｜>                 128813  <｜tool▁output▁end｜>
  128800  <｜fim▁hole｜>              128814  <｜tool▁sep｜>
  128801  <｜fim▁begin｜>             128815  <image>          <-- THE image token
  128802  <｜fim▁end｜>               128816  <|ref|>
  128803  <｜User｜>                  128817  <|/ref|>
  128804  <｜Assistant｜>             128818  <|det|>
  128805  <|EOT|>                     128819  <|/det|>
  128806  <｜tool▁calls▁begin｜>      128820  <|grounding|>
  128807  <｜tool▁calls▁end｜>        128821  <td>
  128808  <｜tool▁call▁begin｜>       128822  </td>
  128809  <｜tool▁call▁end｜>         128823  <tr>
  128810  <｜tool▁outputs▁begin｜>    128824  </tr>
                                      128825  <|User|>
                                      128826  <|Assistant|>
  ```

  Plus **800** `<｜place▁holder▁no▁N｜>` (CONTROL, ids 128000..=128799) and
  **453** `[PADnnnnn]` (type 5, ids 128827..=129279) - 830 CONTROL and 453
  type-5 in total. Note the two spellings of the chat markers: the fullwidth
  `<｜User｜>` (128803) and the ASCII `<|User|>` (128825) are DIFFERENT tokens.
- **`tokenizer.ggml.pre = deepseek-v3` is not cosmetic: its digit-run cap is
  `\p{N}{1,3}`, and `QwenBpe::from_gguf` was hardcoding 1.** Confirmed from the
  published `tokenizer.json`, whose first pre-tokenizer `Split` is literally
  `\p{N}{1,3}`; at cap 1 this vocab tokenizes `12345` as five single-digit ids
  instead of the reference's two (`["123", "45"]`), and every number in a
  document's text splits differently. Fixed by
  `data::qwen_tokenizer::digit_run_max_from_pre`
  (`deepseek-v3` → 3, everything else keeps the previous single-digit default;
  `deepseek-llm`'s unbounded `\p{N}+` is deliberately not mapped, it is not a
  `K`). Ten HF-`tokenizers` ground-truth vectors are pinned in the test above,
  including both prompt strings this model ships with; `"Hello" -> [19923]`
  independently agrees with the llama.cpp tokenization `generate.rs` anchors on.
- **The residual pre-tokenizer gap is stated, not silent.** brain's scanner is
  the cl100k/Qwen pattern; DeepSeek-V3's differs in two further places - an
  isolated CJK/kana branch (`[一-龥぀-ゟ゠-ヿ]+`, which splits CJK away from
  adjacent Latin where this scanner keeps one letter run) and `\p{P}\p{S}` where
  this one writes `[^\s\p{L}\p{N}]` (they differ on combining marks and control
  characters). Neither is reachable from the ASCII prompts this model uses, and
  both are documented on `from_gguf`.
- **`is_letter` needed no back-port after all.** The `\p{L}`-vs-`is_alphabetic`
  (`Nl`) mismatch this phase was sent to fix was already ported into
  `qwen_tokenizer.rs` by commit `8e62cbe9` ("F31 partial", 2026-08-10) - the
  definition there is byte-for-byte `clip_bpe.rs`'s. What was missing is the
  regression test, now added
  (`roman_numerals_take_the_digit_branch`: `Ⅻ` takes the digit branch, `ⅠⅠⅠⅠ`
  groups by the cap, ASCII/accented text is unmoved), plus the residual gap
  (1510 codepoints, all `Mn`/`Mc`/`So`/unassigned, 0 in the other direction)
  restated on the definition instead of a stale "not back-ported yet" note.

## Not yet done

- [x] Generic GGUF loader (`crates/gguf`) with two-way coverage, proven
      against both real files.
- [x] Checkpoint-free goldens: `tools/goldens/deepseek_ocr_dump_reference.py
      --out testdata/deepseek-ocr` (no flag needed -- the tiny fixture is the
      default and only implemented path; `--gguf/--mmproj` is a documented
      `NotImplementedError` stub for the real-checkpoint dump). Writes
      `tiny/ckpt/model.safetensors` (116 random tensors, flat HF-like names
      documented in the script docstring), `tiny/golden.safetensors` (114
      per-stage taps) and `manifest-tiny.json`. **Regenerated by the Phase 4
      capstone** -- the numbers below are the CURRENT ones; see "Questions that
      were open" above for what moved and why. Tiny dims deliberately break
      every real-scale coincidence: batch 1; SAM grid 13x7, window 4x3 (divides
      neither extent, so the zero-pad path runs), embed 16 = 2 heads x 8 (16 is
      the smallest dispatchable width at a 12-row window); compressor
      16->6->9->11 giving a 4x2 = 8-token CLIP input; CLIP width 14 = 2 x 7 on a
      NATIVE 3x3 position grid (10 rows) that is bicubically resampled onto 4x2,
      plus a real `pre_norm`; **c_out 11 != clip_width 14** (the concat-order
      gate, which at real scale is 1024 == 1024); projector 25 -> d_model 12 =
      3 heads x 4; decoder 13 tokens, layer 0 dense ff 21, layer 1 MoE 5 experts
      top-2 (raw gates) + 2 shared fused to 14. Self-checks assert in-script:
      rel-pos einsum vs an explicit four-deep loop (1e-6), `F.interpolate`
      linear vs a longhand half-pixel resample, raw vs renormalized router gate
      DIFFER (0.084 on the gate, 0.248 on the layer output) and the layer used
      the raw one, concat halves slice back exactly, pad-token qkv == qkv bias
      exactly, window partition/unpartition round trip, the window/embed binding
      alignment, and that the position resample is not the identity. All three
      rel-pos table cases are exercised (identity, downsample, upsample).
      Note: because `c_out != clip_width`, the fixture carries a
      `clip.patch_bypass` widening Linear that does NOT exist at real scale
      (there the compressor output is the patch token verbatim).
- [x] SAM decomposed relative-position-bias kernels + gradcheck
      (`attn_relpos_qr/add/drh/drw/dq/dr.wgsl`). The bias is never
      materialized: `qr` hoists `q·R` into two `[heads, T, k_ext]`
      intermediates (12.6 MB each at the real global-attention shape, vs
      805 MB for a `[heads,T,T]` slab) and `add` folds them into the existing
      score slab in place, so the whole thing rides the existing
      `block::chunked_bidir_{fwd,bwd}` loop behind ONE `Option` - no second
      copy of the chunk loop, every existing call site passes `None`.
      `get_rel_pos`'s resample+gather adds NO kernel: it is a
      data-independent 2-tap weighted gather (`model::vit::rel_pos_gather`,
      host, unit-tested against hand-computed tables) composed from
      `embed`/`scale_row`/`add2`/`nlc_nchw` forward and `scale_row`/`emb_bwd`
      back. Two dense-table layouts are kept because the forward and backward
      want opposite fastest axes. Gated by `gradcheck::check_deepseekocr_relpos`
      (directional) and `check_deepseekocr_relpos_elementwise` (per-entry on the
      four tables), both green on wgpu/Vulkan AND the CPU Cranelift JIT with no
      "not JIT-compiled" fallback. Mutation-verified: dropping the second
      interpolation tap leaves the DIRECTIONAL check green on `w.rel_pos_h`
      (rel 5.43e-2, inside the gate) while 24 of its 40 entries go red per
      entry - that pair is why both entry points exist.
- [x] Core hoists (Phase 3, all verified, `check_moe`/`check_glm` bit-identical
      before/after): `WindowPlan::padded` (SAM zero-pad window partition, a
      sentinel row so `.inv()` drops pad rows for free); `VitKernelIds::gelu_erf`
      → `mlp_act` rename across all 8 real call sites (pure rename, same kernel
      ids bound); `router_gate.wgsl`/`router_gate_train.wgsl`/`router_bwd.wgsl`
      gain `norm: u32, scale: f32` mirroring the sigmoid router's existing
      pattern, `RouterKind::Softmax` gains `norm_topk_prob`/`routed_scaling`,
      backward has a real branch (no quotient-rule term when `norm=false`), a
      new `softmax_router_gradcheck_unnormalized_and_scaled` gate passes;
      `model::moe::shared_expert_bwd` (both the unweighted and sigmoid-gated
      arms) closes the standing no-backward gap, glm's inline copy is deleted
      and routed through it; `Qwen3`/`Qwen35` gain `img_embeds_buf()`/
      `d_img_embeds_buf()` device-side accessors (additive, `write_img_embeds`
      untouched); `imaging::tiling::internvl_grid` (discrete candidate-ratio
      tiling) lands beside `moondream::select_tiling` (now re-exported from
      there), with a test proving the two policies genuinely disagree on some
      inputs. One pre-existing unrelated test failure found in `brain-mirror`
      (`t8_multiframe_tiny::s3_forward_is_finite`, a NaN) was proven
      pre-existing (reproduces at pristine HEAD) and unrelated to the (2-line,
      same-kernel-id) rename touching that crate.
- [x] `crates/sam1` (SAM-1 ViT-B tower) - patch embed + learned pos embed →
      N pre-LN blocks (windowed via `WindowPlan::padded`, or global) with the
      decomposed rel-pos bias → conv neck (1x1 → LayerNorm2d → 3x3 →
      LayerNorm2d) → the two stride-2 compressor convs, `[1, c_out, g/4, g/4]`
      out. **Adds no kernel and no shared block**: `model::vit`
      (`WindowPlan`/`WindowIndex`/`RelPosAxis`/`RelPos`), `model::block`
      (`chunked_bidir_{fwd,bwd}` with the rel-pos `Option`, `LayerNormIds`)
      and `vision::blocks` (`Conv`, `LayerNorm2d`) compose the whole thing.
      Forward AND a full hand-written backward, gradient-checked over **all 39
      tensors, none frozen** (`crates/sam1/tests/gradcheck.rs`): worst rel
      **1.359e-3** on an Intel Arc (Vulkan) and **1.245e-3** on the CPU
      Cranelift JIT, with **no `not JIT-compiled` fallback** for any kernel
      including all six `attn_relpos_*`. Tiny fixture mirrors the golden
      dumper's SAM geometry (patch 2, grid 13x7, window 4x3 → pads to 16x9,
      2 blocks windowed+global, rel tables 7/11 and 15/13 so identity,
      downsample AND upsample all run), but at `d_model = 16`, not the
      golden's 10 - **10 is not dispatchable**: `chunked_bidir_fwd` binds
      `ctx` sliced at `row0 * d_model` floats, that offset must be 64-float
      (256 B) aligned, and a 12-row window forces `16 | d_model`. Asserted by
      `SamViTConfig::check_bindable`. Mutation-verified, and the mutation
      changed the test suite: summing `attn.qkv.bias` over `rows` instead of
      `attn_rows` (dropping the 53 zero-pad positions, whose k/v ARE the qkv
      bias) is **invisible to the directional check** (rel 6.28e-2, a pass) and
      needed a per-entry gate, where it turns 44 of 48 entries red (worst rel
      1.41). Import reuses `gguf::deepseek_ocr_vision::{config_from_gguf,
      classify}` with a SAM-only filter - no second name table - and is
      coverage-gated against the real shipped mmproj: 476 source tensors → 179
      written, 297 dropped on the record, derived config == the documented
      `SamViTConfig::deepseek_ocr()` preset.
      *(Not done: batch > 1, pos-embed interpolation to a non-native grid.
      **Phase 5 ran the real-weight forward parity** - `tests/parity.rs`
      consumes `sam1::import::weights_from_gguf`, and found the wgpu
      three-or-more-block corruption recorded above, which is why that test
      pins the CPU backend.)*
- [x] `crates/clip::ClipVision` (vanilla CLIP-L/14, `PatchSource` bypass seam) -
      built entirely on `model::vit::vit_block_fwd_cached`/`vit_block_bwd` with
      `mlp_act`/`mlp_act_bwd` bound to `quick_gelu`; `PatchSource::{Pixels,
      Tokens{grid}}` is the bypass seam, proven **bit-identical** across both
      paths on 1664 output floats (both backends), the token half additionally
      cross-checked against a longhand host conv. Learned position embedding
      is bicubic-interpolated to a non-native grid (implemented, not stubbed -
      the whole reason CLIP is in this model), with a full adjoint chain. 35
      tests green; `EvaVision`/`ClipText` proven byte-for-byte unaffected (74
      gradcheck report lines diffed to empty before/after). No post-LayerNorm
      exists in the real checkpoint (only `pre_norm`, confirmed from the mmproj
      tensor list) - the tower output is the last block's hidden state.
      *(Phase 5 confirmed both of those against the shipped file: `v.pre_ln`
      is present, the CLIP-scoped names carry no `post_ln`, and the block
      bodies are exactly two LayerNorms each. Note the trap that check first
      fell into - llama.cpp names the **SAM** blocks' `norm2` `post_ln`, so a
      whole-file search for "post_ln" finds 24 tensors that have nothing to do
      with this tower.)*
      `use_gelu=false` really does mean quick-GELU (the inversion is real and
      still pinned by a test) - **but this checkpoint sets it TRUE**, so the
      quick-GELU reading of *this* file was wrong; see the Phase 5 facts above.
      `ClipVisionConfig` wraps `gguf::deepseek_ocr_vision::ClipConfig`
      directly. ~~Import implemented (prefix-strip + two-way coverage) but not
      yet run against real weights.~~ **Phase 5: run against the real mmproj,
      and a GGUF-native import added beside it** - see the Phase 5 import entry.
      `gradcheck::check_clip_vision` was NOT
      added to `crates/gradcheck` (would require editing a crate outside this
      change's scope while sibling work was in flight there) - the gate lives
      self-contained in `crates/clip/tests/vision.rs` instead; lifting it into
      `gradcheck::clip` is a small follow-up.
- [x] `crates/deepseekv2` (MHA-only DeepSeek-V2 decoder) - modeled on
      `qwen35moe`'s shape (tape-built `Vec<Step>` fwd/bwd, `model::Model`),
      composing `model::block::{rmsnorm,rope,gqa,swiglu}` + `model::moe`
      end-to-end (dense layer 0, MoE layers 1..N via `router_fwd_kind` +
      `expert_fwd`/`moe_layer_bwd` + `shared_expert_fwd`/`shared_expert_bwd`
      with `shared_gate_w: None`). `DeepseekV2Config` wraps
      `gguf::deepseek_ocr::DeepseekOcrConfig` rather than redeclaring it. Real
      (non-stub) GGUF import, ~5 lines per entry point since the loader already
      does the mapping. 14/14 tests green on both backends across 4
      router-policy fixture variants (smooth, smooth+scaling, sparse raw,
      sparse renormalized), worst rel error 2.1e-3-3.0e-3. Two gate defects
      caught and fixed before they could certify anything: `norm_topk_prob` is
      provably inert at `top_k == n_experts` (a no-op renormalization), so
      exercising it requires `top_k < n_experts`, now asserted by
      `router_policy_flags_are_live`; and the production `std=0.02` init made
      the MoE branch numerically invisible against the FD noise floor, fixed
      by using `std=0.15` for non-gain tensors in the gradcheck fixture (worst
      rel error 1.1e-1 → 3.0e-3). LoRA/INT8/sharding/paged-KV serving
      explicitly out of scope for this phase (qwen35moe has all of these;
      this crate is the decoder shape only, not yet the full production
      surface).
- [x] `crates/deepseekocr` composite + `tiny_ref` parity. `config.rs` groups the
      three sub-configs and owns only the projector's two shape facts
      (`projector_in = compressor_out + clip_width`, `projector_out = decoder
      d_model`); `check_real_scale_shaped()` asserts `compressor_out ==
      clip_width` and REFUSES the fixture's widening bridge, so the production
      path cannot inherit fixture plumbing. `encoder.rs` is the DeepEncoder
      (SAM -> NCHW->NLC flatten -> CLIP via `PatchSource::Tokens` -> drop the
      cls row -> `concat2` -> `Linear` projector), forward AND backward.
      `model.rs` splices the projector output into the decoder at a caller-given
      `(row0, n_rows)` and runs to logits, forward AND backward. `rows.rs` is
      pure host index math (see above), 8 unit tests, consumed by nothing yet.
      **`crates/deepseekv2` gained the splice seam** (`enable_mm_splice` /
      `write_img_embeds` / `read_d_img_embeds` / `img_embeds_buf` /
      `d_img_embeds_buf` over `model::vlm::splice_{fwd,bwd}`, appended
      `splice`/`splice_bwd` pipelines) plus per-stage read taps; **`crates/sam1`
      gained a composition seam** (`write_image`, `d_out`, `d_image` and the
      per-stage taps a composite's parity test reads) -- both purely additive.
      The golden was REGENERATED (batch 2->1, `sam_embed` 10->16, CLIP
      `pre_norm` added, the position table moved onto a native 3x3 grid with a
      real bicubic resample); all in-script self-checks stay green.
      **31 taps at cosine 1.0000000000 on both the CPU Cranelift JIT and an
      Intel Arc (Vulkan)**, worst max_abs 4.2e-5 (`dec_l1_out`, GPU).
      Mutation-verified three ways: swapped concat order, transposed
      compressor flatten, and `norm_topk_prob = true` each turn it red.
- [x] **Real-weight STAGE parity (Phase 5).** Oracle: llama.cpp itself - the
      upstream consumer this GGUF format targets - with its `common/debug.cpp`
      graph-eval callback patched to dump FULL tensor data, run against the very
      same shipped Q8_0 pair. Two curated captures, converted into the repo's
      own fixture format by
      `tools/goldens/deepseek_ocr_convert_llamacpp_dump.py` (its header carries
      the capture commands, the byte-layout checks and the semantic findings) →
      `testdata/deepseek-ocr/real/{vision,decoder}.safetensors` +
      `manifest-real.json`. `crates/sam1/tests/parity.rs` and
      `crates/deepseekv2/tests/parity.rs` are the gates; both self-skip.
      *Vision* (`llama-mtmd-debug -p encode --image gray -n 1024`): SAM's 12
      block outputs + the neck/compressor output + CLIP's assembled input.
      **All 14 taps ≥ 0.9994**, worst `sam_blk07_out` 0.99944 / max_abs 1.65e-1,
      `sam_output` 0.99996 / 5.23e-3.
      *Decoder* (`llama-eval-callback --prompt "Hello" -n 1`, tokens `[0,
      19923]`): **27 taps ≥ 0.99957** (worst `attn_out_L04`), `embd` exact
      (cos 1.0 / max_abs 0), `result_output` 0.99981, and **the greedy next
      token agrees: 3 == 3**. Router internals: **32 taps ≥ 0.99992**, expert
      SELECTION identical on every row of every MoE layer.
- [x] **Real-weight IMPORT for the whole composite (Phase 5).** All three towers
      plus the projector now load from the shipped GGUFs and are exercised on
      them. `crates/clip` gained `import::gguf_mmproj::{config_from_gguf,
      dry_run, weights_from_gguf}` - the mmproj-side twin of the existing
      safetensors prefix strip, shaped exactly like `sam1::import` (the loader's
      own classifier narrowed to one tower, everything else a recorded
      `Mapped::Dropped`), so reading the CLIP branch no longer requires
      expanding the whole 476-tensor file to fp32 on disk first.
      **476 -> 293 written, 183 dropped** (293 = 5 stem + 24 x 12), config
      asserted equal to the corrected `ClipVisionConfig::deepseek_ocr()` preset.
      `crates/clip/tests/deepseek_ocr_real.rs` also runs the tower on the
      **reference's own patch tokens** (`clip_input_tokens[1..]` from the
      fixture): all 263 168 outputs finite, RMS climbing 0.065 (`x0`) -> 0.139
      (blk0) -> 0.186 (blk11) -> 0.231 (blk23), range [-3.97, 7.87]. There is no
      reference to compare that output against, so no threshold is claimed for
      it. The identity position resample at 16x16 is asserted exact, and
      `class_embed` matches the reference's assembled row 0 at max_abs 0.
- [x] **Real-weight COMPOSITE forward (Phase 5, sanity only).**
      `crates/deepseekocr/tests/real_weight.rs` runs image -> SAM -> CLIP ->
      concat -> projector -> splice -> decoder -> logits on the real pair. The
      SAM stage inside the composite reproduces the SAM-only parity number
      exactly (0.9999586 / 5.23e-3), the compressor->CLIP handoff matches the
      reference, the splice places the projector output **verbatim** (max_abs
      0), and the logits are finite with a plausible distribution (33.4 M
      values, range [-20.47, 20.09], rms 3.31; top-5 next-token logits
      15.95/15.83/15.83/15.21/14.40 - a real, non-degenerate distribution).
      Nothing downstream of the compressor is gated, because nothing downstream
      was captured.
      **Two API gaps found, both real-scale-only and both blocking Phase 6:**
      (1) `DeepseekOcr::new`/`DeepEncoder::new` take `&HashMap<String,
      Vec<f32>>`, so a 2.9 B decoder needs its ~12 GB fp32 expansion resident in
      host memory *and* on the device simultaneously - `DeepseekV2::new_on`
      already takes a `&dyn checkpoint::TensorSource` and streams, the composite
      has no equivalent; (2) `DeepseekOcr::new` hardcodes `train = true`, adding
      gradients and AdamW moments for those parameters (~47 GB total) - there is
      no inference build of the composite at all. The test assembles the two
      halves by hand for exactly these reasons, and drops the encoder before
      building the decoder so peak host stays at one model's worth.
      **Both closed by Phase 6a below; that test now builds one real
      `DeepseekOcr`.**
- [x] **Inference build of the composite + a streaming init seam (Phase 6a).**
      The two gaps the Phase 5 entry above recorded are closed, and the numbers
      here are measured (`/proc/self/status` `VmHWM`/`VmPeak` around the real
      construction, CPU backend, this 30 GiB box), not estimated.

      *What changed, exactly:*
      * `sam1::SamEncoder` had **no** train/infer split at all - it called
        `ParamStore::new` (every tensor `Role::Trainable`) unconditionally and
        always allocated the backward scratch. It now has
        `SamEncoder::new_on(gpu, cfg, src: &dyn checkpoint::TensorSource, seed,
        train)` - shaped after `DeepseekV2::new_on`, the existing precedent -
        plus `new_inference(..)` and the unchanged `new(gpu, cfg, &init, seed)`
        (= `new_on(.., true)`, same signature, same behaviour). The
        per-block backward scratch moved into a `BlockBwd` and the tower-level
        one into a `SamBwd`, both `Option`, both `None` when `train = false`.
        The **SSA forward cache is NOT conditional** and must not be: `ln1`,
        `wm`, `qkv`, `ctx`, `scores`, `probs`, `proj`, `attn_out`, `res`,
        `ln2`, `h`, `h2`, `mlp_out`, `out` are what the *forward's own* step
        list writes and reads, not a backward convenience.
      * `clip::ClipVision::new_on_src(gpu, cfg, b, patch_src, weights, train)`
        - the builder `new_on`/`new_train_on` already delegated to, now public
        and over a `TensorSource`. `ClipVision`'s role/`bwd` split already
        existed; the composite was simply calling `new_train_on`
        unconditionally.
      * `DeepEncoder::new(dev, cfg, init: &dyn TensorSource, seed, train)` and
        `DeepseekOcr::new(dev, cfg, init, seed, seq, row0, train)` /
        `DeepseekOcr::new_split(dev, cfg, vision, decoder, seed, seq, row0,
        train)`. `new_split` exists because the shipped checkpoint really *is*
        two files (a 448 MB mmproj, a 3.1 GB LM GGUF) and merging them into one
        map is the thing being removed. The hardcoded `true` in
        `DeepseekV2::new_on` is gone.
      * Call sites decided per what each needs: `tiny_ref`'s forward-parity
        gate and `sam1/tests/parity.rs` build **inference** (so both now also
        prove the frozen build records the same forward graph);
        `tiny_ref`'s backward/descent gate and `sam1/tests/gradcheck.rs` build
        **trainable**.

      *Measured, SAM ViT-B alone at its production 1024x1024 shape*
      (`crates/sam1/tests/parity.rs`, same weights, same input, **14/14 taps
      bit-identical between the two builds** - the report diffs to empty):

      | build | VmHWM (resident) | VmPeak (allocated) |
      |---|---|---|
      | `train = true`  | 5.81 GiB | 10.69 GiB |
      | `train = false` | 4.41 GiB | **6.15 GiB** |

      The two columns differ because on the **CPU backend** an untouched
      buffer costs address space, not RSS - a forward-only run never writes the
      backward scratch, so `Role::Frozen` (grad + 2 AdamW moments dropped) is
      what shows in RSS while the skipped scratch shows in `VmPeak`. On a real
      GPU the `VmPeak` column is the one that is VRAM, so **4.54 GiB per SAM
      tower** is the number that matters there.

      *Measured, the whole composite on the real Q8_0 pair*
      (`crates/deepseekocr/tests/real_weight.rs`, seq 258, **256** image rows -
      a 16x16 token grid at rows `[1, 257)`, plus BOS and one trailing text row;
      an earlier draft of this entry said 257 and was off by one):
      * `train = false`: **completes**. VmHWM **24.39 GiB** (transient, during
        construction), steady RSS 21.0 GiB after the forward, minimum system
        `MemAvailable` 5.04 GiB, 116 s wall. Every Phase 5 number reproduces
        exactly - `sam_output` cos 0.9999586 / max_abs 5.23e-3, splice max_abs
        0.0, the same top-5 logits 15.947/15.834/15.833/15.212/14.402.
      * `train = true`: **does not fit, measured not argued.** RSS climbed
        monotonically to 24.1 GiB in ~21 s of *construction* - it never reached
        the end of the constructor, let alone a forward - and was still rising
        at ~1.2 GiB/s when the guard killed it with `MemAvailable` at 6.0 GiB.
        Arithmetic agrees it cannot land: 4x(0.4 B encoder + 2.9 B decoder)
        params is ~53 GB of parameter state before any activation.

      **So: a real single-forward pass now runs on a 30 GiB box, and Phase 6's
      parity work is unblocked.** Streaming import is what makes the encoder
      half cheap and the decoder half possible; see the next item for the piece
      that is deliberately NOT done.
- [ ] **Stream the decoder straight from the Q8_0 GGUF** (the remaining half of
      the import story, scoped OUT of Phase 6a rather than forced). Everything
      now takes a `checkpoint::TensorSource`, and `checkpoint::gguf::MmapGguf`
      *is* one - but it is keyed on **llama.cpp's** tensor names
      (`blk.N.ffn_gate_exps.weight`, …), not brain's, and one GGUF expert-stack
      tensor fans out to 64 brain tensors. So a GGUF-native composite build
      needs a `checkpoint::remap::RemapSource` plan
      (`Fetch::Whole`/`Fetch::Slice`) derived from the classifier
      `gguf::import::to_map` already drives - i.e. a `remap_plan` sibling of
      `to_map` in `crates/gguf/src/import.rs`, with its own two-way coverage
      gate. Until that exists, the decoder half streams from the cached
      **11.7 GB fp32 `.safetensors` expansion** on disk (built once by
      `crates/deepseekv2`'s parity test) rather than from the 3.1 GB shipped
      file, and the vision half still goes through `gguf::import::to_map`,
      an eager ~1.6 GiB host map held for the whole `new_split` call. Removing
      both would take the composite's 24.39 GiB construction peak down by
      ~1.6 GiB and drop the 11.7 GB disk requirement entirely. Neither blocks
      Phase 6.
- [x] **Composed-loop / generation parity against the real Q8_0 weights
      (Phase 6b) - this FREEZES `crates/deepseekv2/src/model.rs`'s
      forward/decode contract.** Every real-weight number above it is ONE
      forward over a FIXED token sequence. That is a snapshot, and it cannot see
      the three things only a loop can get wrong: whether RoPE's position
      argument advances as tokens are appended, whether the causal mask stays
      correct as the sequence grows, and whether feeding a generated token back
      in produces the right next distribution. All three are now gated.

      *The new API - purely additive, nothing existing changed:*
      * `DeepseekV2::generate_greedy(&self, prompt_ids: &[u32], n_new: u32) ->
        Vec<u32>`, returning prompt + continuation as ONE sequence (so
        `result[i + 1]` is the greedy argmax after `result[0..=i]`).
      * `DeepseekOcr::generate_greedy(&self, image: &[f32], prompt_ids: &[u32],
        n_new: u32) -> Vec<u32>` - the image is encoded **once** and every step
        re-splices the same projected tokens, so the 400 M-parameter DeepEncoder
        does not re-run per token.
      * The plan budgeted for rebuilding the model (or the tape) at each new
        length. Neither is necessary: **nothing is rebuilt per step**.
        `logits_all` already
        re-derives the whole forward step list at a caller-given `t_use <=
        self.t`, so the loop is `n_new` `logits_all` calls against ONE instance
        sized for the final length. The per-step cost is a host-side
        `Vec<Step>`; no device buffer is reallocated.
      * `O(T²)` recompute on purpose. This is the recompute tier of the
        `generate` / `generate_kv` pair `crates/gpt`/`crates/glm`/`crates/qwen3`
        keep; the cached tier stays out of scope with the rest of the serving
        surface, and a *correctness* proof wants the tier that shares its graph
        with `forward()` rather than a second graph. Ties break to the lowest id
        (strict `>` scanning up), which is llama.cpp's own greedy rule.

      *The oracle.* A locally built llama.cpp on the same shipped
      `DeepSeek-OCR-Q8_0.gguf`: `llama-cli -p "Hello" -n 8 --temp 0 --top-k 1
      -no-cnv --single-turn` emits `!How can I help you today?`, which
      `llama-tokenize --ids` maps back to `[3, 4117, 588, 342, 1694, 440, 4316,
      33]` (reproducible - run twice, identical). With the prompt tokenization
      `[0, 19923]` in front, the reference is
      `[0, 19923, 3, 4117, 588, 342, 1694, 440, 4316, 33]`. This is **anchored,
      not a fresh claim**: index 2 (`3`) is exactly the greedy next token
      `real_lm_decoder_matches_llamacpp` already proves against a byte-level
      `result_output` dump, so the multi-step chain starts from a step verified
      against tensors rather than from a CLI's stdout.

      *Result:* `crates/deepseekv2/tests/generate.rs` -
      **all 10 positions agree exactly, position by position.** CPU backend,
      35-45 s wall including the 12 GB load. The failure path is a debugging aid,
      not an opaque `assert_eq`: it names the FIRST divergent index and re-runs
      that step to print the top-5 candidates with their logits, because a
      near-miss and a wrong model are different bugs.

      *Measured memory* (`/proc/self/status`, Phase 6a's method), decoder alone,
      inference build, sized for 10 tokens:

      | point | VmRSS | VmHWM | (second run) |
      |---|---|---|---|
      | after construction | 10.38 GiB | 19.91 GiB | 8.48 / 19.59 GiB |
      | after all 8 greedy steps | 10.62 GiB | **19.91 GiB** | 9.13 / **19.59** GiB |

      Two runs are shown because the RSS column moves with page-cache pressure
      (the expansion is mmapped); the quantity that does NOT move is the one
      under test - **the loop adds +0.2 to +0.7 GiB of RSS and ZERO additional
      peak.** The brief's worry, that rebuilding the tape per step could
      multiply Phase 6a's already-tight cost, does not materialise: there is no
      per-step device allocation to multiply. The ~19.6-19.9 GiB peak is
      construction (streaming the 11.7 GB expansion) - Phase 6a's number, not a
      generation cost.

      *Fast lane too.* `deepseekv2::model::tests::
      greedy_decode_is_prefix_stable_and_reads_the_last_row` pins the loop's
      bookkeeping at toy dims on both backends with no checkpoint at all: prompt
      returned verbatim, a run resumed from a generated prefix producing the
      same ids as a straight-through one (so no state survives a step), each id
      the argmax of the **last** row -
      plus a guard that rows 0 and `t-1` still disagree, so that last claim
      cannot quietly go vacuous the way it would at `t = 1`.

      *Stretch goal, DONE: the composed loop on the whole real composite.*
      `crates/deepseekocr/tests/real_weight_generate.rs` (its own test binary -
      cargo runs binaries one at a time, so two ~21 GiB composites can never be
      resident together) runs image → SAM → CLIP → concat → projector → splice →
      decoder and then 3 greedy steps, at the production 1024x1024 / 258-row
      prompt shape (`seq = 261`, image rows `[1, 257)`). **Passes, 190.7 s,
      VmHWM 21.44 GiB (all in construction), RSS 19.87 GiB at the end, minimum
      system `MemAvailable` 4.2 GiB.** Generated ids `[372, 223, 643]`; the
      deciding top-5 is `643 @ 9.237, 19 @ 8.853, 20 @ 8.614, 21 @ 8.529,
      23 @ 8.415` - a real, non-degenerate distribution.
      There is **no exact oracle here and none is claimed** (llama.cpp's debug
      callback segfaults inside this model's CLIP graph, and a `llama-mtmd-cli`
      run would give comparable TEXT at best, not ids, since the two
      preprocessors differ). What IS asserted beyond "it ran": every logit of
      the deciding forward is finite, the distribution is language-model-shaped,
      and - the one real gate - **causal self-consistency across every step**.
      With a correct causal mask, position `i`'s logits cannot depend on tokens
      after `i`, so the single length-260 forward the loop ends on must
      reproduce, at each position, exactly the argmax the step-time forward
      chose over the shorter prefix. It does, for every step, and it costs zero
      extra forwards.

      *Tech-debt paid on the way:* the model-store lookup / fp32-expansion /
      CPU-pin / inference-build scaffolding moved to
      `crates/deepseekv2/tests/common/real_lm.rs` and is now shared by
      `parity.rs` and `generate.rs` (the house `#[path]` pattern `crates/yolo`
      and `crates/omni` already use), so the two real-weight binaries cannot
      drift about *which* weights they ran - which is what lets `generate.rs`
      treat `parity.rs`'s verified argmax as its anchor. The `/proc/self/status`
      memory reporter was hoisted to `brain_testutil::mem` before it became a
      second copy.

      *Still open after this phase* (unchanged by it): incremental/KV-cached
      decode, sampling beyond greedy, EOS/stop handling, batch > 1, and the
      whole serving surface - see the items below.
- [x] **Real image preprocessing (Phase 7a).** `crates/deepseekocr/src/
      preprocess.rs` - arbitrary decoded RGB in, the `[3, 1024, 1024]`
      normalized NCHW tensor `DeepEncoder::forward` takes out. Until now that
      entry point had no producer at all: every test in this effort fed either a
      spatially constant fill or a slice normalized by hand, so "the model
      accepts an image" was not something the repo could do.

      ```rust
      pub fn preprocess_image(gpu: &Gpu, cfg: &DeepseekOcrConfig,
                              hwc: &[f32], w: u32, h: u32, fit: Fit) -> Vec<f32>
      pub fn preprocess_to(gpu: &Gpu, hwc: &[f32], w: u32, h: u32,
                           out_w: u32, out_h: u32, fit: Fit) -> Vec<f32>
      pub fn placement(fit: Fit, w: u32, h: u32, out_w: u32, out_h: u32)
              -> (u32, u32, Border)
      pub const NORMALIZATION: imaging::Normalization
      ```

      `hwc` is the repo's wire format for a decoded image (`[h, w, 3]`
      interleaved f32 in `[0,1]`, what `capability::blob::decode_image` hands
      back). Adds **no kernel**: `imaging::Ctx` does the resize (`resize_bicubic`),
      the per-channel affine (`film_chan`) and the pad (`pad2d`); only the
      HWC→CHW permutation is host glue, per the `crates/imaging` rule.

      **Two things the brief for this phase asserted are WRONG, and both were
      settled from the shipped file rather than argued:**
      * **The normalization is `mean = std = 0.5`, not OpenAI CLIP's
        `[0.48145466, …] / [0.26862954, …]`.** The shipped
        `mmproj-DeepSeek-OCR-Q8_0.gguf` carries
        `clip.vision.image_mean = clip.vision.image_std = [0.5, 0.5, 0.5]`, and
        llama.cpp's `mtmd_image_preproc_out::append_overview` applies exactly
        those. Borrowing CLIP-L's own published constants would have been wrong
        for this checkpoint however right it looks - the CLIP tower here never
        sees a pixel, it runs on the compressor's token grid. So the constant is
        `imaging::Normalization::HALF` (the `[0,1] → [-1,1]` rescale that
        already had a name), and a skip-if-absent unit test asserts it against
        the real GGUF so it cannot drift. This also **confirms** the Phase 5
        note that the `-p encode` debug buffer is unnormalized: a real gray page
        maps to 0.0, not to 0.5.
      * **The global view is an aspect-preserving centred fit-and-pad, not a
        stretch.** llama.cpp's `mtmd_image_preprocessor_deepseekocr::preprocess`
        resizes with `PAD_NEAREST` and `image_pad_color = {127, 127, 127}` -
        upstream's `ImageOps.pad(img, (1024, 1024), color=(127,127,127))`, where
        the pad colour is `round(mean * 255)`. `Fit::Pad` is that geometry and is
        the default; `Fit::Stretch` (the plain resize) is kept because a square
        source makes the two identical, so a test that only ran squares would not
        be testing the geometry at all.

      **A third thing the brief did not anticipate, found by running it: the
      cubic filter RINGS, and the reference cannot.** llama.cpp's resampler
      writes a `clip_image_u8`, so 8-bit saturation clamps its result to `[0,1]`
      before the normalization ever runs. Cubic interpolation has negative lobes
      and overshoots at a hard edge - which is what a document scan is made of.
      Measured on the synthetic page at 1600x1131 → 1024²: the normalized
      output spanned **`[-1.176, 1.191]`** against the checkpoint's `[-1, 1]` -
      a ~9.6 % overshoot of the source range, entirely at the text edges.
      The real-weight test's range assertion is what caught it. So `preprocess`
      clamps, as `1 - relu(1 - relu(x))` - **no new kernel**: `relu_inplace` plus
      the per-channel affine, four dispatches over the resized image, negligible
      beside the resize that produced it. `preprocess::PIPELINES` is therefore
      `imaging::PIPELINES` plus `relu_inplace`; `imaging` has no clamp of its
      own, and adding one there is a separate change.

      **The pad falls out for free, and that is why the order is
      resize → clamp → normalize → pad.** `imaging::Ctx::pad_zero` can only fill
      with zero (`pad2d.wgsl` has no `pad_value` word), which is not a constraint
      here: normalize BEFORE padding and zero *is* the mean-grey border. (The
      reference quantises its border to `127/255` before normalizing, so its
      padded pixels land at `-0.00392` rather than at `0.0` - a ~0.4 % offset
      confined to the letterbox bars, and ours is the exact value upstream's
      `int(mean * 255)` is an 8-bit approximation of.)

      *Confirmed from the real code, as the brief asked:* **1024 is not a
      literal** - it is `sam.grid_h * sam.patch_size` via
      `sam1::SamViTConfig::image_h()`/`image_w()` (there is no `image_size()`;
      the tower carries independent h/w). And **there is one normalization, not
      two**: `DeepseekOcrConfig::check_real_scale_shaped` asserts
      `compressor_out == clip_width` and refuses the fixture's widening bridge,
      i.e. the compressor output IS the CLIP patch token verbatim, so there is
      exactly one pixel-facing stage and exactly one affine.

      *Known deviation, recorded not claimed away:* the reference resamples with
      Pillow's antialiased bicubic (`RESIZE_ALGO_BICUBIC_PILLOW`); brain's
      `resize_bicubic` is the torch/ONNX cubic (`a = -0.75`, half-pixel) and does
      **not** antialias on a downscale. Nothing gates on the difference because
      no reference capture exists for a non-constant image. Closing it would mean
      a new kernel in `crates/imaging`, which is out of this change's scope.

      *Scope:* the **global (overview) view only**. The Gundam-mode local tile
      grid at 640² has its row layout in `rows.rs` but still no consumer.
      Batch 1, like the rest of the crate.

      *Gates.* Six checkpoint-free unit tests (Intel Arc / Vulkan, sub-second):
      the production 1024² shape and `[-1,1]` range from a 640x427 gradient
      (3 145 728 floats, range within `[-1, 1]`, and explicitly **not** `[0,1]`);
      a four-solid-quadrant source downscaled 8x, each quadrant's centre matching
      its own colour through the affine on all three channels - one assertion
      that catches a transposed resize, a flipped axis and a CHW/HWC mixup; the
      letterbox pair (a 2:1 source pads top/bottom to exactly zero under
      `Fit::Pad` and produces **no** zero row under `Fit::Stretch`, asserted
      together because "zeros somewhere" only means letterbox if the other mode
      lacks them); the hard-edge/ringing gate (a two-solid-halves source at the
      extremes of the unit interval, which has no headroom for an overshoot to
      hide in - and which also asserts both extremes are still REACHED, so it
      cannot pass by blurring the edge away; the smooth gradient above never
      leaves range and would let a missing clamp through); the odd-gap centring
      rule (the reference's `round(gap/2)` rounds a half AWAY from zero, so the
      extra pixel goes top/left - the opposite of `imaging::Border::centre`,
      which is why that helper is not reused); and the mean/std assertion against
      the real mmproj.

      *Real weights.* `crates/deepseekocr/tests/real_weight_image.rs` - its own
      test binary, CPU backend, `#[ignore]`d to the slow lane. A synthetic
      1600x1131 document page (text-line bands, a vertical rule, a saturated
      three-channel margin block - structured rather than noise, which would
      resize to a flat mean and be nearly as blind as a constant fill) goes
      through `preprocess_image` into the real 400 M-parameter DeepEncoder, and
      on into the full composite when the fp32 expansion is on disk (guarded on
      a live `MemAvailable` read, so a small box prints a skip instead of
      OOM-killing whatever else is running). **No byte-level oracle exists and
      none is invented** - the reference capture was taken on the constant fill,
      and llama.cpp's debug callback segfaults inside this model's CLIP graph
      anyway. What IS asserted: the letterbox bars are exactly zero and the
      content band is not; every encoder output float is finite; and - the one
      real gate - **the embedding actually depends on the picture**, measured
      against the constant-gray page every other real-weight test in this crate
      feeds. A pipeline that silently dropped its input (a zeroed upload, a
      mis-sized buffer, a resize that collapsed) would still be finite and still
      be the right shape; that comparison is what would catch it.

      *Result: PASSES, CPU backend, 137.5 s, both stages.* Preprocessing landed
      3 145 728 floats, all finite, range exactly **`[-1.0000, 1.0000]`** (the
      clamp holding), rms 0.684; the letterbox put a **1024x724** content band
      inside 150-row top and bottom borders (1131·1024/1600 = 723.8, the 300-row
      gap split evenly). The encoder ran: `clip_spatial` 262 144 finite,
      `[-1.987, 1.139]`, rms 0.207; `projector_out` 327 680 finite,
      `[-2.030, 1.294]`, rms 0.100. **Page vs constant-gray embedding: cosine
      0.550, max_abs 1.313** - the picture reaches the model. The composite then
      ran to logits: the splice placed the projector output verbatim (cos
      1.0000000000, max_abs **0.0**), **33 354 240 logits, all finite**,
      `[-20.47, 21.90]`, rms 3.70, deciding top-5
      `18935 @ 14.519, 22233 @ 13.821, 99170 @ 13.770, 5 @ 13.593,
      91664 @ 13.512` - a real, non-degenerate distribution, and a **different**
      one from the constant-gray page's (`15.947/15.834/…` in `real_weight.rs`),
      which is the same "the image reaches the model" property one stage further
      down. Memory: encoder-only **VmHWM 7.17 GiB** (1.59 after the mmproj
      import, 3.44 built, 7.17 after the forward) - so the vision half fits a far
      smaller box than the composite - and the whole composite **VmHWM 24.26 GiB
      / RSS 21.33 GiB**, matching Phase 6a's 24.39 GiB to within noise. An
      earlier attempt on the same box **skipped stage 2** and said why
      (`MemAvailable` 14.5 GiB against the 16 GiB floor, another job holding the
      RAM), which is the guard working rather than a flake.

      *Tech debt paid on the way:* the mmproj-import / model-store / CPU-pin /
      `describe` scaffolding was copy-pasted in **two** real-weight binaries and
      was about to become three. It now lives once in
      `crates/deepseekocr/tests/common/real_vision.rs` (the house `#[path]`
      pattern, mirroring `crates/deepseekv2/tests/common/real_lm.rs`), and
      `real_weight.rs` / `real_weight_generate.rs` were migrated onto it. A
      hand-rolled cosine/max-abs helper in the new test was likewise dropped for
      the existing `brain_testutil::parity::compare`.
- [x] **The real tokenizer and a real prompt (Phase 7c).**
      `crates/deepseekocr/src/prompt.rs`: `tokenizer_from_gguf(path)` (the LM
      GGUF's own `tokenizer.ggml.*` KV through `QwenBpe::from_gguf` - header
      only, the 3.1 GB mmap is dropped on return), `ImageTokens` (the
      `rows::Src` kind → token id map; all three kinds are `<image>` = 128815 in
      this checkpoint, and the struct exists so no call site hardcodes that
      coincidence), and

      ```rust
      pub fn build_prompt(tok: &QwenBpe, text_before: &str, text_after: &str,
                          tokens_per_side: u32) -> Result<Prompt, String>
      ```

      returning `Prompt { ids, row0, n_rows, plan }` -
      `BOS ++ text_before ++ 273 image rows ++ text_after` for the real
      `tokens_per_side = 16`, with `image_run() -> (row0, n_rows)` in
      `enable_mm_splice`'s shape. `ViewGrid::global_only()` only, because the
      splice takes ONE contiguous run. Text still carrying `<image>` is an
      `Err`, not a placeholder id outside the spliced window whose embedding is
      never written. Tests: three checkpoint-free unit tests on a synthetic
      gpt2-scheme vocab (deliberately non-real ids, so a hardcoded 128815
      fails), plus `tests/prompt_real.rs` on the shipped file - all green, and
      the reference prompt
      `<image>\n<|grounding|>Convert the document to markdown.` comes out as
      **283 ids**: BOS, the 273-row image block at `[1, 274)`, then 9 text ids.
      Cost: the KV block only, 0.2 s and no GPU.

      **Two things in the brief turned out wrong, both recorded above:** there
      are no separate newline/view-separator token ids to find (one id covers
      all three row kinds), and `plan.runs()` on a global-only plan is **16**
      runs of 16, not one - a newline breaks the *projector-side* contiguity at
      every token row. The **decoder** side is one contiguous run, which is what
      the splice needs, and that is what `n_rows` reports.

      **The gap this opens, and it is real:** `n_rows` is **273** (256 projector
      rows + 16 `image_newline` + 1 `view_separator`), while `DeepseekOcr` still
      sizes its splice at `cfg.image_tokens()` = **256**. Every real-weight
      number in this file was measured on that 256-row prompt, i.e. on a
      sequence with no newline/separator rows - which is NOT what the reference
      feeds its decoder. Closing it means assembling a 273-row embedding block
      (scatter the projector output over `plan.runs()`, broadcast the mmproj's
      `vision.image_newline` / `vision.view_separator` rows into the other 17)
      and sizing the splice from the plan instead of from `image_tokens()`. That
      is a `model.rs`/`encoder.rs` change, it is the next step, and until it
      lands the prompt builder and the composite do not meet.
      **CLOSED by Phase 7d, below** - `DeepseekOcr::new_with_prompt` sizes the
      splice from the prompt and `deepseekocr::layout::RowGather` fills it. The
      256-row numbers above are still correct for the path they were measured
      on (`new`/`new_split`), which still ships.
- [x] **The real 273-row image block (Phase 7d) - the gap Phase 7c flagged is
      closed.** `build_prompt` emitted a 273-row image run while `DeepseekOcr`
      spliced 256, and the two learned vectors the mmproj ships
      (`vision.image_newline`, `vision.view_separator`) had **no consumer
      anywhere in `crates/deepseekocr`** - `glue_param_list()` did not even
      declare them, so the import silently dropped both. Now:

      ```rust
      // crates/deepseekocr/src/config.rs
      pub const IMAGE_NEWLINE:  &str = "vision.image_newline";   // [d_model]
      pub const VIEW_SEPARATOR: &str = "vision.view_separator";  // [d_model]
      // both now in DeepseekOcrConfig::glue_param_list()

      // crates/deepseekocr/src/layout.rs  (new module)
      pub const LAYOUT_PIPELINES: &[(&str, &str)];   // splice, embed, emb_bwd
      pub struct RowGatherIds { pub splice: usize, pub embed: usize, pub emb_bwd: usize }
      pub struct RowGather;
      impl RowGather {
          pub fn new(g: &Gpu, rows: &[Src], projector_rows: u32, d_model: u32) -> RowGather;
          pub fn build_fwd(&self, g: &Gpu, ids: RowGatherIds, proj_out: &DeviceBuffer,
                           newline: &DeviceBuffer, separator: &DeviceBuffer,
                           steps: &mut Vec<Step>);
          pub fn build_bwd(&self, g: &Gpu, ids: RowGatherIds,
                           d_newline: &DeviceBuffer, d_separator: &DeviceBuffer,
                           steps: &mut Vec<Step>);
          pub fn rows(&self) -> u32;                       // 273
          pub fn projector_rows(&self) -> u32;             // 256
          pub fn shared_row_counts(&self) -> (u32, u32);   // (16, 1)
      }

      // crates/deepseekocr/src/encoder.rs
      pub fn row_gather(&self, rows: &[Src]) -> RowGather
      pub fn forward_rows(&self, image: &[f32], rg: &RowGather) -> Vec<f32>   // [273, d_model]
      pub fn backward_rows(&self, d_block: &[f32], rg: &RowGather) -> Vec<f32>

      // crates/deepseekocr/src/model.rs
      pub fn new_with_prompt(dev: DeviceFactory, cfg: DeepseekOcrConfig,
                             vision: &dyn TensorSource, decoder: &dyn TensorSource,
                             seed: u64, seq: u32, prompt: &Prompt, train: bool) -> DeepseekOcr
      pub fn generate_greedy_from_prompt(&self, image: &[f32], prompt: &Prompt,
                                         n_new: u32) -> Vec<u32>
      pub fn generate_greedy_from_prompt_cb(&self, image: &[f32], prompt: &Prompt,
                                            n_new: u32, on_token: impl FnMut(u32)) -> Vec<u32>
      pub fn row_gather(&self) -> Option<&RowGather>
      ```

      **Additive, not a replacement.** `DeepseekOcr::new`/`new_split` still
      splice `cfg.image_tokens()` contiguous rows - that is the checkpoint-free
      golden fixture's own scope and every parity number above was measured on
      it. `new_with_prompt` sizes `enable_mm_splice` at the PROMPT's `n_rows`
      and fills it through `RowGather`. Both constructors funnel through one
      private `build`, so the only thing that differs is `rows: Option<RowGather>`.

      **Adds no kernel** - it is a row-table gather:
      `table = [proj_out (256) | image_newline | view_separator]` assembled with
      three `splice` copies (`model::vlm::splice_fwd`, the decoder's own splice
      seam, reused rather than re-derived - the one new dependency this adds,
      `brain-model`, is for exactly that), then one `embed` gather over a 273-entry
      index vector. That is `model::vit::gather_rows`' ABI.

      **The adjoint is two different kernels, and that is the point.** The index
      map is NOT a permutation (16 rows read one vector), so it splits by row
      kind: every projector row is used *exactly once* (asserted at construction
      - a layout that dropped or duplicated one would silently lose or double a
      gradient), so restricted to them the map is a bijection and its adjoint is
      the inverse **gather** (`embed`, `P*d` threads, no clear); the two shared
      vectors take `emb_bwd`'s accumulating scatter, summing 16 and 1 terms
      respectively. Same shape as `model::vit::RelPosAxis::build_bwd`'s table
      adjoint and the same contract - the destination is a parameter gradient,
      so `zero_grads` clears it, not the module. (`row_scatter` for the
      projector half would also work; it costs `P*d*n_rows` comparisons against
      the gather's `P*d` reads, so the gather is what ships.)

      **Tests.**
      * `deepseekocr::layout::tests` (5, checkpoint-free, 0.5 s): every
        assembled row asserted **row by row** against its source at exact
        equality - a swapped newline/separator or an off-by-one projector index
        still yields the right shape and the right multiset of rows, so an
        aggregate check would pass it; the adjoint asserted the same way, with
        an explicit "only the LAST newline row was accumulated" negative; the
        accumulate-until-cleared contract; a directional finite difference of
        the backward against the forward over all three inputs
        (`projector_out` rel **1.86e-5**, `image_newline` **9.25e-6**,
        `view_separator` **5.61e-5**); and the three refusals
        (duplicate / missing / out-of-range projector row).
      * `tests/tiny_ref.rs::the_real_layout_splices_the_learned_rows_verbatim_and_trains_them`
        (checkpoint-free): the composite through `new_with_prompt` on the tiny
        fixture's `4x2` grid - 13 rows, all bit-identical to their source after
        the splice - then the two learned rows finite-differenced through the
        WHOLE composite loss.
      * `tests/real_weight_layout.rs` (`#[ignore]`, slow lane): the real
        tokenizer's real prompt (`<image>\n<|grounding|>Convert the document to
        markdown.` - **283 ids, image run [1, 274)**), the real mmproj, the real
        273-row block, on the synthetic scanned page. *PASSES, CPU, 145.1 s,
        both stages.* `vision.image_newline` `[-0.0991, 0.0718]` rms 0.01762 and
        `vision.view_separator` `[-0.0674, 0.0591]` rms 0.01741 - two distinct,
        non-zero, finite vectors, so an equality that held on zeros is
        impossible. `image_block` **349 440 floats, all finite**,
        `[-2.0299, 1.2941]` rms 0.09735, and **256 projector + 16
        `image_newline` + 1 `view_separator` rows exact** - before the splice
        (out of `forward_rows`) and again after it (out of the decoder's own
        `res[0]`). Memory: encoder-only VmHWM **7.22 GiB**, composite **24.09
        GiB**, both within noise of `real_weight_image.rs`'s 7.17 / 24.27.
        **The observation worth recording, and it is NOT gated**: the deciding
        top-5 is `128816 @ 23.87, 1 @ 20.66, 114761 @ 17.10, 5 @ 17.05,
        15 @ 16.13` - id **128816 is `<|ref|>`**, the token that OPENS a
        grounded reference, which is exactly what a `<|grounding|>` prompt
        should elicit first. No oracle says it must be, so nothing asserts it;
        but the 256-row runs, whose prompts were `vec![0; seq]` filler, produced
        nothing like it.

      **The one thing worth recording about the composite gradcheck**: at the
      glue check's own `eps = 5e-4` the separator's directional derivative
      (~1.1e-3, ONE row of 13) moved an fp32 loss near 4.2 by ~1.4e-6 - three
      ULP - and the check read `1.4305e-3` against an analytic `1.1095e-3`,
      rel **2.2e-1**. It was measuring fp32 quantization, not curvature.
      `eps = 5e-2` is what clears the quantum; the second-order error that buys
      back is what the 5e-2 gate absorbs. Recorded because the failure mode
      (a gradcheck that fails on a CORRECT gradient because the loss cannot
      resolve the step) looks exactly like a real bug.

      **Two things the brief for this phase got wrong, both settled from the
      code:** (1) the adjoint is *not* uniformly `emb_bwd`-style
      scatter-accumulate - the projector half is a plain inverse gather, because
      `rows.rs` already guarantees the bijection; (2) `glue_param_list()` was
      not the only place to touch - declaring the two tensors makes them
      *required* by `ParamStore::new_with_roles_src` (which panics on a missing
      init weight), so `tests/tiny_ref.rs`'s two-way-covered init map had to
      supply them. The golden dump is single-view and contiguous and has no such
      tensors, so they are zero-filled there - the same treatment
      `patch_embed.weight` already gets on the bypass path - and the contiguous
      path's test now ASSERTS their gradients are exactly zero, which is the
      honest statement that they are not in that graph.

      **Regression, measured on both sides rather than asserted.** Every
      existing number in this file is reproduced to the last digit after the
      change (the contiguous path shares one encoder forward with the new one,
      so this is the check that it really was left alone):
      `deepseekv2/tests/parity.rs` 27 taps worst cosine **0.9995669542** at
      `attn_out_L04`, `result_output` **0.9998140528** / max_abs 3.209e-1,
      `embd` exact, greedy next token 3 == reference; the router gate test 32
      taps worst **0.9999152198** at `moe_weights_L05[1]`;
      `deepseekv2/tests/generate.rs` all 10 positions agreeing,
      `[0, 19923, 3, 4117, 588, 342, 1694, 440, 4316, 33]`;
      `deepseekocr/tests/real_weight.rs` `sam_output` cos **0.9999585967** /
      max_abs 5.232e-3, splice max_abs **0.0**, logits `[-20.4686, 20.0925]`
      rms 3.31114, top-5 `15.94673/15.833976/15.832762/15.211724/14.402454`;
      `real_weight_generate.rs` ids **[372, 223, 643]**, deciding top-5
      `643 @ 9.2369585, 19 @ 8.853098, 20 @ 8.613617, 21 @ 8.528564,
      23 @ 8.41503`; `real_weight_image.rs` range exactly `[-1.0000, 1.0000]`
      rms 0.68409, 1024x724 content band in 150-row borders, page vs
      constant-gray **cos 0.550161 / max_abs 1.313**, splice max_abs **0.0**,
      logits `[-20.4686, 21.8969]` rms 3.69726, top-5
      `18935 @ 14.518797, 22233 @ 13.82066, 99170 @ 13.769826, 5 @ 13.592821,
      91664 @ 13.511839`. Fast lane: 30 unit + 3 `tiny_ref` tests green, with
      `tiny_reference_stage_parity`'s 31 taps unchanged (worst cosine
      1.0000000000, `logits` max_abs 3.600e-5) and the backward gate's
      `composite loss 4.221861 -> 4.205152 (glue grad norm 1.6707)` identical.

      *Still open* (unchanged by this): multi-view (Base/Gundam) prompts, which
      need one `enable_mm_splice` call per decoder run - `RowGather` itself is
      already indifferent to tiles; and everything in the items below.
- [x] **Per-token streaming callback on both greedy loops (Phase 7b).** Both
      loops ran to completion and returned everything at once, so a served path
      could not emit a token until the whole decode was done - the D-Bus/HTTP
      `Progress::token` surface the item below needs is impossible on that
      shape. Closed by the pattern `crates/qwenvl` already uses
      (`Qwen3Vl::generate` / `generate_cb`), copied verbatim rather than
      invented: the loop moves into a `_cb` variant taking an extra
      `impl FnMut(u32)`, the old name becomes a thin `|_| {}` wrapper.

      ```rust
      // crates/deepseekv2/src/model.rs
      pub fn generate_greedy(&self, prompt_ids: &[u32], n_new: u32) -> Vec<u32>
      pub fn generate_greedy_cb(&self, prompt_ids: &[u32], n_new: u32,
                                on_token: impl FnMut(u32)) -> Vec<u32>

      // crates/deepseekocr/src/model.rs
      pub fn generate_greedy(&self, image: &[f32], prompt_ids: &[u32],
                             n_new: u32) -> Vec<u32>
      pub fn generate_greedy_cb(&self, image: &[f32], prompt_ids: &[u32],
                                n_new: u32, on_token: impl FnMut(u32)) -> Vec<u32>
      ```

      The composite's `_cb` forwards straight through to the decoder's, so the
      encode/splice half still runs ONCE, before the first callback - the vision
      tower is not a per-token cost and nothing about it is observable through
      this seam. `on_token` fires immediately after each id is appended:
      exactly `n_new` times, never for a prompt id. **The asymmetry a serving
      caller must get right** is that the RETURN value still carries the prompt
      ahead of the generated ids (unchanged, and the two existing tests depend
      on it), so the stream is `result[prompt_ids.len()..]`, *not* `result`.

      *Behaviour-preserving, measured on both sides rather than asserted.* The
      real-weight gate `crates/deepseekv2/tests/generate.rs::
      real_lm_greedy_decode_matches_llamacpp` returns the same ten ids
      `[0, 19923, 3, 4117, 588, 342, 1694, 440, 4316, 33]` - all 10 positions
      agreeing with llama.cpp - before (114.8 s) and after (75.5 s; the wall
      difference is page-cache warmth on the 11.7 GB expansion, not decode).
      `model::tests::greedy_decode_is_prefix_stable_and_reads_the_last_row`
      passes unchanged in both. The composite gate
      `crates/deepseekocr/tests/real_weight_generate.rs` re-ran too, since the
      wrapper it calls is the half that was restructured: same ids
      `[372, 223, 643]`, same deciding top-5 to the last digit
      (`643 @ 9.2369585, 19 @ 8.853098, 20 @ 8.613617, 21 @ 8.528564,
      23 @ 8.41503`), 176 s, VmHWM 21.32 GiB.

      *The new fast-lane test, and the check that did NOT work.*
      `model::tests::generate_greedy_cb_emits_each_token_as_it_is_produced`
      (toy dims, no checkpoint) collects the streamed ids and asserts the count
      is `n_new` and the ids are `generate_greedy`'s tail. That much is the easy
      half. The hard half is that a callback which **drains the finished vector
      after the loop** passes every id-and-count assertion and is still useless
      - nothing arrives one instant earlier than the return value does. The
      first attempt at catching it had the callback re-derive its own token from
      "the ids seen so far"; that **passed the mutation**, because `logits_all`
      is a pure function of its prefix and therefore agrees with itself under
      either ordering. What does separate them is the decoder's WORK: the
      callback reads the device handle's online dispatch counter
      (`Gpu::ops_counters().steps`, armed by `reset_ops_counters`; counters are
      per-handle and `gpu_core::testgpu::dev` hands every caller its own, so a
      concurrent test cannot contribute), and the test demands it have strictly
      grown between consecutive calls. Mutation-verified in both directions -
      the drain implementation reports `[330, 330, 330, 330, 330]` and fails.

      *Still open* (unchanged by this): the callback is synchronous and
      infallible, so there is no cancellation and no early stop through it -
      EOS/stop handling, sampling beyond greedy and KV-cached decode remain
      where the Phase 6b entry left them.
- [x] **Serving contract (Phase 7d-bis) - `brain caps`, `brain do`, the
      residency scheduler, D-Bus and the OpenAI/Anthropic surfaces, from ONE
      catalog entry.** The three things that had to exist first were the real
      273-row prompt (above), the per-token callback (below) and a *production*
      checkpoint loader - the last of which did not exist: the only
      real-weight-loading code in the tree was `tests/common/real_vision.rs`,
      test glue that a served path could not call. So it was promoted rather
      than copied.

      ```rust
      // crates/deepseekocr/src/import.rs   (new - production, not test glue)
      pub const STORE: &str;      // "ggml-org/DeepSeek-OCR-GGUF"
      pub const MMPROJ: &str;     // "mmproj-DeepSeek-OCR-Q8_0.gguf"
      pub const LM: &str;         // "DeepSeek-OCR-Q8_0.gguf"
      pub const EXPANDED: &str;   // "DeepSeek-OCR-brain-fp32.safetensors" (derived)
      pub struct Files { pub dir, mmproj, lm, expanded: PathBuf }
      impl Files { pub fn locate(dir: impl AsRef<Path>) -> Result<Files, String>; }
      pub fn encoder_weights(mg: &MmapGguf)     -> Result<HashMap<String, Vec<f32>>, String>;
      pub fn encoder_weights_from(mmproj: &Path)-> Result<HashMap<String, Vec<f32>>, String>;
      pub fn expand_lm(lm: &Path, expanded: &Path) -> Result<String, String>;
      pub fn decoder_reader(files: &Files)      -> Result<WeightReader, String>;
      pub fn config(files: &Files, block_size: u32) -> Result<DeepseekOcrConfig, String>;
      pub fn tokenizer(files: &Files)           -> Result<QwenBpe, String>;

      // crates/deepseekocr/src/caps.rs   (new - the serving surface)
      pub const MODEL: &str = "deepseek-ai/DeepSeek-OCR";   // case-exact upstream repo
      pub const DIR_VAR: &str = "BRAIN_DEEPSEEK_OCR_DIR";
      pub const DEFAULT_INSTRUCTION: &str = "<|grounding|>Convert the document to markdown.";
      pub const SEQ_LEN: u32 = 512;
      pub const DEFAULT_MAX_NEW: i64 = 32;
      pub fn generate_spec() -> ActionSpec;   // streaming, messages/prompt/max_new/weights, image in, text out
      pub fn manifest() -> Manifest;          // .with_max_context_tokens(SEQ_LEN)
      pub struct Session;                     // the built composite + tokenizer + preprocess device
      impl Session {
          pub fn load(dir: &str) -> Result<Session, String>;
          pub fn dir(&self) -> &str;
          pub fn generate(&self, inv: &Invocation, progress: &mut dyn FnMut(Progress)) -> ActionResult;
      }
      pub struct DeepseekOcrProvider;         // from_env() / new(dir) -> Option
      ```

      **`config` derives the shape from the two files and THEN refuses anything
      that is not `DeepseekOcrConfig::deepseek_ocr`.** Deriving alone would
      silently serve a re-quantized checkpoint of a different shape; hardcoding
      alone would not notice one at all.

      **The four `tests/real_weight*.rs` binaries were migrated, not
      duplicated.** `tests/common/real_vision.rs` keeps only what is genuinely
      test glue (the model-store lookup, the CPU pin, the synthetic page, the
      tensor summaries); its `encoder_weights` is now a one-line wrapper over
      `import::encoder_weights` and its path constants are re-exported from
      there. Every existing assertion in those binaries is unchanged.

      **The CPU-backend pin became a declaration, not an env write.** The tests
      set `BRAIN_DEVICE=cpu`, which is right for a single-threaded binary that
      owns the process and wrong for a resident that lives as long as the
      server: it would change the backend every *other* model builds on
      afterwards. Instead `DeepseekOcrResident::estimate` reports a **RAM-only**
      `MemCost` (`vram == 0`, 22 GiB - rounded up from the 21.32 GiB VmHWM the
      composite gate measures), which is `residency::place::pick_device`'s own
      vocabulary for "skip the GPU class entirely", and
      `caps::Session::load` builds every stage with `gpu_core::Gpu::new_cpu`.
      `activate` refuses a non-CPU assignment loudly rather than building a
      tower that would produce plausible garbage.

      ```rust
      // crates/cli/src/resident_deepseekocr.rs   (new)
      pub struct DeepseekOcrResident;   // from_env() / new(dir) -> Option
      // ResidentModel::{manifest, instance_key, estimate, activate} + Instance::run
      // run_batch: the serial default (per-image encoder pass, no decoder batch axis)

      // crates/cli/src/catalog.rs - ONE ModelEntry, which is what wires
      // `brain caps`, `brain do`, `resident.rs::build_executor`'s
      // `models.extend(catalog::residents())`, D-Bus and the HTTP surfaces at once.
      ```

      **Token accounting is real, and the precedent was wrong.** Neither
      `omni::caps::GenerateAction::run` nor `qwenvl::caps::GenerateAction::run`
      sets `prompt_tokens`/`completion_tokens`/`finish_reason`, and
      `apiserve::bridge::read_outcome` defaults them to `0`/`0`/`"stop"` - so
      both report zero usage over the OpenAI and Anthropic APIs today. This
      action sets all three: `prompt.ids.len()`, the ids actually kept (EOS
      included, ids past it excluded), and `qwen3::chat::SeqState::finish`'s own
      stop/length rule minus the tool-call and cancellation arms this model has
      no notion of.

      *Still open* (and stated on `docs/models/deepseek-ocr.md`): the greedy
      loop does **not** stop at EOS - the output is truncated and
      `finish_reason` is honest, but the wall time is always `max_new` steps
      (now `O(1)` decode steps, not full recomputes - see the KV-cache entry
      below), because `deepseekv2`'s per-token callback is synchronous and
      infallible. No cancellation for the same reason. No batching, greedy
      only, single global view, CPU only.
- [x] **Performance pass (Phase 8): profiled, and the dominant cost fixed.**
      `BRAIN_PROFILE` on a real `--max_new 32` run (a real document page, the
      default prompt) found the decode loop's `O(T²)` recompute exactly where
      the earlier phases already suspected: every generated token re-ran the
      whole grown sequence through all 12 layers, so a 283-prompt-token run
      cost ~22 s per additional token and `--max_new 32` took roughly twelve
      minutes.

      **The fix: `DeepseekV2::generate_greedy_kv`/`step`, a real KV cache -**
      the `O(T)`-per-token twin `crates/gpt`/`crates/glm`/`crates/qwen3`
      already keep alongside their own `O(T²)` recompute tier, now built for
      this MoE decoder too. The prompt still pays ONE batched forward (which
      also handles the vision-language splice and seeds the persistent
      per-layer K/V cache via `model::block::kv_cache_fill`'s documented
      bulk-prefill pattern); every token after that is one incremental
      attention step (`model::block::gqa_decode_step`, the same primitive
      `crates/gpt`/`crates/glm`/`crates/qwen3`/`crates/qwen35moe` already use)
      plus a single-row MoE/dense FFN pass. Adds **one** new kernel wiring
      (`kernels::ROPE_AT`, for rotating at an explicit absolute position -
      `rope_base.wgsl`'s `row % tcols` convention cannot express a single new
      row past position 0) plus the existing `KV_APPEND`/`ATTN_DECODE_SCORES`/
      `DECODE_SOFTMAX`/`ATTN_DECODE_APPLY` quartet - no new WGSL source at
      all. `caps::Session::generate` (the served path) was switched onto it.

      **Measured, same machine, same real weights, `--max_new 32`:** total
      wall dropped from ~12 min (`O(T²)`, extrapolated from the `--max_new
      2`/`--max_new 10` numbers on `docs/models/deepseek-ocr.md`) to ~123-147 s
      (`O(T)`) - roughly a 5-6x wall-clock improvement. `BRAIN_PROFILE`'s own
      per-kernel table for the KV-cached run: `moe_linear_gated` 66.9% (one
      prompt-prefill forward + 32 decode steps combined), `matmul` 10.6%,
      `matmul_reg3` 9.7%, the rest (attention decode-step kernels, `silu_mul`,
      `scale_add`, `rope_at`, `kv_append`, ...) each under 5%.

      **Gated both ways**, per this repo's TDD convention: a toy-dims fast-lane
      test (`generate_greedy_kv_matches_recompute`, exercising both the dense
      and MoE MLP branches) and a real-weight extension of the existing
      llama.cpp-anchored gate (`tests/generate.rs`) - the KV path reproduces
      the EXACT same ids the `O(T²)` recompute path already verifies against
      llama.cpp on the real `DeepSeek-OCR-Q8_0` weights.

      **One optimization tried and MEASURED, not shipped:** row-compacted MoE
      (skip dispatching the ~58/64 non-selected experts per decode step,
      mirroring `crates/glm`'s `model::moe::expert_fwd_compact`, which measures
      ~7x on GLM's own *batched* MoE forward). At a single decoded row this
      needed a host readback of the router gate per MoE layer to know which
      experts to skip. Measured: `moe_linear_gated` call count dropped
      67584 -> 8250 (~8.2x), but its own total time was UNCHANGED (~23.3 s
      either way) and the whole decode's profiled total went UP (34.9 s ->
      39.3 s) - the per-row gate check already makes a non-selected expert's
      dispatch cheap on this shape, so the real cost was the selected
      experts' compute all along, and the extra host round-trips (11 MoE
      layers x 32 tokens = 352 of them) cost about as much as the skipped
      dispatches saved. Reverted per the "re-measure the WHOLE pass, not the
      per-kernel table" rule - a per-kernel call-count win is not the same
      thing as a whole-pass win.

      **Head-to-head vs `llama-mtmd-cli`, same machine, same real weights,
      same image, same prompt, `--max_new 32`, greedy (`--temp 0 --top-k
      1`):** `llama-mtmd-cli` completes in ~20.6 s (its own vision-encode
      stages log ~15 s of that); brain's KV-cached run takes ~123-147 s. Not
      a win yet - the KV-cache fix closed the loop that was structurally
      quadratic, but with decode no longer dominant, model construction
      (~18 s: streaming the fp32 decoder expansion + the mmproj import) and
      especially the vision encoder (~75-80 s: SAM ViT-B at 1024x1024 ->
      CLIP-L/24 -> compressor -> projector, NOT profiled per-kernel this
      pass) are now the bottleneck, and neither was touched this session.
      `llama-mtmd-cli` encodes the same-shaped image roughly 5x faster.
      **The next profiling pass belongs on the vision encoder, not the
      decode loop** - re-profiling after a fix is what promotes the next
      bottleneck, and this is that promotion. See
      `docs/performance/overview.md`'s case study for the full numbers.
- [x] **Vision-encoder performance pass (Phase 8 follow-up): the promoted
      bottleneck, profiled and partly closed.** Before touching a kernel,
      measured how much of the ~75-80 s vision-encode estimate is the CPU
      backend `crates/sam1` is pinned to (the wgpu backend corrupts this
      tower's output at production scale - a separate, still-open defect,
      NOT fixed here) vs a genuine algorithmic cost: a new weight-free bench
      (`crates/sam1/src/bin/sam1_bench.rs`, `SamViTConfig::deepseek_ocr()` at
      real 1024x1024/12-block geometry with random weights) measured the CPU
      pin at **~3.6x** (72.2 s CPU vs 20.0 s wgpu-as-timing-reference, best
      of 3) - a real number now attached to whoever eventually closes the
      wgpu bug, not a guess.

      **Per-kernel breakdown (`BRAIN_PROFILE`, the CPU backend's own
      accumulator) found `attn_apply_cross` at 70-71%** of the whole tower's
      CPU forward - 20-30x more wall time than `attn_scores_cross` at the
      IDENTICAL total FLOP count per call. Root cause: `attn_apply_cross`
      transposes V into `vt[hd,tk]` with a scatter-write loop (`hd=64`
      elements per source row, each landing in a DIFFERENT destination row
      16 KiB apart - 64 cache lines touched per source row, not revisited
      until 63 unrelated lines have been touched, for all 4096 source rows
      at the global-attention shape); `attn_scores_cross`'s own packing is a
      plain contiguous per-row copy and pays none of this.

      **Two fixes landed, both re-verified against the whole relevant test
      matrix (`cargo test -p brain-sam1 --release`, `-p brain-backend-cpu
      --release`, `-p brain-clip --release`, `-p brain-deepseekocr
      --release`, `-p brain-model --release`, `-p brain-deepseekv2
      --release` - all green, gradcheck and parity included):**
      1. `crates/sam1` now dispatches its forward GEMMs through
         `model::block::pick_gemm` (matching `crates/clip`/
         `crates/deepseekv2`) instead of a hardcoded naive `matmul` index -
         per kernels.md's "already-faster-sibling" check, but measured to
         have **zero effect on the CPU backend**: `backend-cpu` already
         routes every `matmul*` kernel name to the same native AVX2+rayon
         `fast_ops::matmul_abt`, so the naive-vs-tiled distinction this
         selector exists for was never live on this backend for this tower.
         Kept as a correctness-neutral consistency fix, real on any backend
         where that kernel-family collapse does not hold.
      2. `backend-cpu::fast_ops::attn_apply_cross`'s V-transpose is now
         tiled (buffer 16 source rows, then write the destination
         cache-line-major instead of interleaved with 63 unrelated lines).
         Same math, unit-tested against the direct definition. **Isolated,
         same-process A/B: 1.0x-2.0x, never worse.** Whole-tower effect NOT
         cleanly resolved this session - this machine ran under sustained,
         heavy, variable load from a second agent's concurrent work for most
         of the pass (`uptime` load average measured between ~1 and ~32 on a
         22-core box), and a full-tower before/after pair taken under that
         noise moved 68.8 s -> 72.2 s, inside the noise floor this machine
         was producing, not a clean signal either way.

      **New per-stage instrumentation, not just a number.**
      `DeepEncoder::run_forward` now brackets SAM / the NCHW->NLC bridge /
      CLIP / the projector separately under `BRAIN_PROFILE` (previously one
      lumped "encode+splice+decode" line), shared via a `pub(crate)
      stage_time` hoisted to `crates/deepseekocr/src/lib.rs`. Two real runs
      (same checkpoint, same document image, a few minutes apart): vision
      encode measured directly at **25.3 s and 34.4 s** (SAM 23.8-33.0 s,
      CLIP 1.4-1.5 s, the bridge/projector each under 15 ms) - replacing the
      prior pass's undifferentiated "~75-80 s" estimate with a real,
      per-stage number, independent of whether the two kernel fixes above
      moved it.

      **Updated head-to-head vs `llama-mtmd-cli`, same session:** two
      same-window pairs (brain 95.6 s vs `llama-mtmd-cli` 70.2 s = 1.36x;
      brain 97.1 s vs 50.9 s = 1.91x) vs the prior pass's 123-147 s vs
      ~20.6 s (~6-7x) on a quieter machine. Read honestly, not
      optimistically: `llama-mtmd-cli`'s OWN number moved 2.5-3.4x slower
      than its previously-recorded baseline in these same runs, proving
      BOTH sides' absolute numbers are inflated by this session's shared
      machine load, not that brain caught up to llama.cpp's true speed. The
      RELATIVE gap narrowing (~6-7x -> ~1.4-1.9x under matched conditions)
      is real and is overwhelmingly the vision-encoder work above, not the
      two kernel fixes' individually-unresolved magnitudes.

      **Still open:** the wgpu correctness bug (out of scope, now has a real
      3.6x cost-of-CPU-pin number attached); model construction's 20-28 s
      (reconfirmed, inspected for redundant work - none found in
      `crates/deepseekocr/src/import.rs`'s streaming path - but not profiled
      per kernel); the tiled-transpose fix's whole-pass magnitude, which
      needs a quiet machine to pin down; and a genuinely clean, single-tenant
      head-to-head number. See `docs/performance/overview.md`'s case study
      for the full per-stage and per-kernel tables.
- [x] LoRA training + measured-descent smoke test. Reused, not reinvented: the
      exact `qwen3::model::Qwen::lora_fwd`/`proj_bwd` shape (already ported
      once for `qwen35moe`) - frozen base `Role::Frozen` + trainable
      `.lora_a`/`.lora_b` `Role::Trainable`, two matmuls + `axpy` forward, the
      matching `matmul_dx`/`matmul_dw`/`grad_scale` backward - composed
      entirely from `crates/deepseekv2`'s OWN already-registered kernels
      (`matmul`/`matmul_dx`/`matmul_dw`/`grad_scale`); only `axpy` needed a new
      pipeline slot in that crate's `PIPELINES`, no new kernel anywhere.
      `deepseekv2::config::DeepseekV2Config::lora` (`qwen3::LoraCfg` reused
      as-is, not redeclared - same choice `qwen35moe` made for the identical
      type) targets the four attention projections
      (`q_proj`/`k_proj`/`v_proj`/`o_proj`), never an MoE expert or the
      router. TDD: the composite descent test was written and confirmed RED
      (compile failure - `deepseekocr::train` and `DeepseekV2Config::lora`
      did not exist) before any implementation landed, then brought GREEN.
      Gradient-checked (`deepseekv2/tests/gradcheck.rs::
      grads_match_finite_differences_lora`, `directional_check` - deliberately
      NOT `elementwise_check`, because a LoRA `A`/`B` pair is read and written
      by exactly ONE matmul chain per targeted projection, not folded/summed
      across several call sites the way T5's `rel_bias` is; that is the same
      choice `qwen3::gradcheck::check_qwen_lora`/`qwen35moe::gradcheck::
      check_qwen35_lora` already made for the identical shape). The composite
      smoke test (`deepseekocr/tests/tiny_ref.rs::
      composite_lora_backward_freezes_the_base_and_descends`) is modelled
      directly on `composite_backward_reaches_the_image_and_descends`'s own
      shape: same tiny checkpoint-free fixture, forward, `zero_grads`,
      forward+backward, then a plain (non-Adam) gradient step - but asserts
      three things that test does not need to: reading the decoder base's
      gradient panics (`Role::Frozen` really allocated no grad buffer, not
      merely "unused"), `decoder().param_names()` is EXACTLY the `.lora_a`/
      `.lora_b` set, and a decoder-base tensor plus a vision-encoder tensor
      are BYTE-IDENTICAL before and after the step. `crates/deepseekocr/src/
      train.rs` is the one new composite-level file (`lora_init_map`, merging
      a real/fixture base weight map with a fresh zero-`B` adapter init,
      mirroring `qwen3::finetune::finetune`'s own merge) -
      `DeepseekOcr::new`/`new_split` needed **zero** changes: `cfg.decoder.lora`
      threads straight through `DeepseekV2::new_on`'s existing role
      assignment. `deepseekv2::lora` (save_adapter/fold_adapter_into) is a
      direct port of `qwen3::lora`/`qwen35moe::lora` for completeness, not
      exercised by the smoke test itself. All new/changed tests green on the
      pooled test device; `qwen3::LoraCfg` gained `PartialEq` (additive) so
      `DeepseekV2Config`'s existing derive keeps working. *Not done*: a
      `finetune`-style CLI verb, a masked-dataset training loop, adapter
      save/load through `brain do` - this phase proves the wiring descends,
      not a production fine-tune (see `docs/models/deepseek-ocr.md`).
- [x] **CPU AVX2/AVX-512 fast paths for the Phase 8 hot kernels.** Phase 8's
      own per-kernel table put `moe_linear_gated` at 66.9% of the KV-cached
      decode loop, running as the scalar one-invocation-per-element
      Cranelift-JIT loop the whole time - `crates/backend-cpu` had a proven
      AVX2-fast-path pattern (`fast_conv.rs`/`fast_ops.rs`, runtime-gated on
      `avx2_available()`) for the base GEMM, conv2d family and cross-attention
      only; this landed native paths for the two next-highest-value kernel
      families on that list, same pattern, not a new one.
      **`moe_linear_gated`{,`_dx`,`_dw`}** (`crates/backend-cpu/src/
      fast_ops.rs::moe_linear_gated_{fwd,dx,dw}`) - the forward reuses the
      SAME `row_abt_avx2`/`row_abt_avx512` microkernel `matmul_abt` already
      uses (a row-gated variant, not a new GEMM); the two backward siblings
      reuse a new shared `axpy` SAXPY primitive (`dst += scale*src`, AVX2+FMA)
      that also gave `moe_linear_gated_dx`'s accumulation loop real
      vectorisation it did not have as scalar-only before.
      **The self-attention family** (`gqa_scores`/`attn_softmax`/`gqa_apply`
      + the `gqa_bwd_{dscores,dv,dq,dk}` backward quartet) - the plain causal
      GQA/MHA attention `deepseekv2`'s own decoder dispatches for its
      prompt-prefill forward (only the cross-attention twin had a native path
      before). Per-head GEMM packing reusing `matmul_abt`, mirroring the
      existing `attn_scores_cross`/`attn_apply_cross` pattern exactly - see
      `fast_ops.rs`'s own module doc for the derivation, including the reason
      `gqa_bwd_dscores`'s causal masking needs no explicit branch (a
      non-routed `probs[i,j]==0` term already zeroes the product).
      **AVX-512 tier**: `fast_conv::avx512_available()` (F+VL+DQ, mirrors
      `avx2_available()`'s exact runtime-detection shape) gates a
      `row_abt_avx512` microkernel (structurally identical to the proven
      `row_abt_avx2`, 16-wide `__m512` lanes) wired ahead of the AVX2 check in
      both `matmul_abt` and `moe_linear_gated_fwd`. **Honesty note, not a
      claim of verification**: this development machine (Intel Core Ultra 7
      155H) does not implement AVX-512 at all (confirmed via `/proc/cpuinfo`),
      so `avx512_available()` is always `false` here - the tier compiles and
      its own unit test (`row_abt_avx512_matches_scalar_when_available`)
      explicitly prints "AVX-512 not available, skipping" and returns rather
      than silently passing as if it had verified the vector logic. It has
      never executed on real hardware in this session.
      **Correctness**: every new kernel gated against a scalar reference
      mirroring its own WGSL kernel's exact formula (not `matmul_abt` with a
      post-hoc mask - the row-gating logic itself is under test), the same
      bit-approximate/fp-reassociation-tolerance style the existing
      `fast_ops.rs` tests already use. Full regression green after landing:
      `cargo test -p brain-backend-cpu --release` (31 passed), `-p
      brain-deepseekv2 --release` (15+8 passed, including the real
      `grads_match_finite_differences_*` gradchecks and
      `generate_greedy_kv_matches_recompute` which exercises the KV-cached
      decode path these kernels serve), `-p brain-model --release` (every
      test binary, incl. `moe_sparse_bwd_parity`/`moe_compact_parity`/
      `vit_windowed`'s 11 tests). `-p brain-sam1 --release` run last, since
      SAM's own attention doesn't dispatch any of these three families
      directly - see that run's own result below.
      **Measured (microbenchmark, not the full pipeline - see below for why)**:
      `moe_linear_gated` forward at the real decoder shape
      (`d_model=1280, moe_ff=896, 64 experts top_k=6`, `m=283` prompt-prefill
      rows, `crates/backend-cpu/src/fast_ops.rs::moe_linear_gated_bench`,
      `cargo test -p brain-backend-cpu --release moe_linear_gated_bench --
      --ignored --nocapture`): AVX2 ran 2.4x-6.1x faster than a
      scalar-with-the-same-rayon-threading baseline across five repeated runs
      on this shared, contended machine (a sibling agent's own parallel
      `cargo test -p brain-model` with LTO was running concurrently
      throughout measurement - 10+ rustc processes at times, swap fully
      saturated - which is exactly the variance window that range reflects;
      re-run on an idle box for a tighter number). A single-decode-row (`m=1`) variant was
      attempted and DROPPED: at that shape the whole call is a few KFLOPs,
      small enough that this workspace's LTO release build could prove the
      repeated near-identical-input call loop-invariant and hoist it out of
      the timing loop even past `black_box` guards on both arguments and a
      per-iteration input perturbation, reading a physically impossible
      ~10-30 TFLOP/s. Shipping a number that could not be trusted was judged
      worse than shipping none - see that bench's own doc comment.
      **`gqa_scores`/`gqa_apply`** at `deepseekv2`'s real prefill shape
      (`bsz=1, heads=10, T=283, head_dim=128` MHA, `crates/backend-cpu/src/
      fast_ops.rs::gqa_family_bench`): AVX2 ran 6.4x (`gqa_scores`) and 15.5x
      (`gqa_apply`) faster than a single-threaded scalar reference matching
      the WGSL kernels' own math, on the same contended machine. Caveat this
      number honestly the other direction from the `moe_linear_gated` one
      above: the comparator here is single-thread scalar, not
      scalar-with-the-same-rayon-threading (unlike `moe_linear_gated_bench`'s
      apples-to-apples baseline) - `gqa_scores`/`gqa_apply` both call
      `matmul_abt` per head, which threads internally once a head's own GEMM
      clears its size threshold, so part of this multiplier is parallelism
      the old scalar JIT path *also* gets (`backend-cpu` always shards
      invocations across the rayon pool, AVX2 or not - see `lib.rs`'s
      dispatch fallback). The wall-clock difference is real (that JIT
      fallback path is what these kernels replace either way), but do not
      read this specific multiplier as "AVX2 alone."
      **No real-weight end-to-end DeepSeek-OCR decode re-run** (the
      `--max_new 32` benchmark this Phase 8 entry's own numbers come from):
      the real `DeepSeek-OCR-Q8_0`/`mmproj` GGUF pair is not present on this
      machine this session (only an empty HF-cache ref stub, no downloaded
      blobs), so the ~22 GiB resident real-weight run this repo's own
      convention calls for could not be reproduced here. The whole-pass
      number this entry's Phase 8 table implies - `moe_linear_gated` at 66.9%
      of a 34.9 s decode - is NOT re-measured; only the kernel-level
      microbenchmark above is. Whoever next has the real weights available
      should re-run `BRAIN_DEEPSEEK_OCR_DIR=<dir> BRAIN_PROFILE=1 brain do
      deepseek-ai/DeepSeek-OCR generate --max_new 32 --in image=page.ppm
      --json` and update this entry and `docs/performance/overview.md`'s case
      study with the real before/after wall-clock and per-kernel table.
      Stayed scoped to `crates/backend-cpu` (plus the ten touched `.wgsl`
      headers' `@cpu: yes -> native` field, required by `make
      kernels-table/check` once `crates/backend-cpu/src/lib.rs`'s `FastIdx`
      gains a kernel's name - the generator derives `native` from that struct,
      not from a separate list, so a fast path and its catalogue entry cannot
      drift apart) - no `crates/deepseekocr`/`crates/sam1`/`crates/deepseekv2`
      source changes. SAM's decomposed relative-position kernels
      (`attn_relpos_*`) were assessed but not landed this pass (out of time
      budget after the two higher-priority families + the AVX-512 tier); they
      remain scalar-JIT on CPU.
Full plan: `/home/user/.claude/plans/woolly-beaming-avalanche.md` (this
session's approved plan; not repo-relative, kept here only as a pointer for
whoever picks this up in the same environment).
