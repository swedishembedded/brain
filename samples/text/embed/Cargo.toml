[package]
# Package names under samples/ are derived from the path:
# `samples/text/embed` -> `sample-text-embed`. The Makefile's
# `samples/%/{build,run}` rules depend on that mapping; `make check/samples`
# enforces it.
name = "sample-text-embed"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sample application: embed text with brain::EmbeddingPipeline, including a real 32k-token document."
publish = false

[dependencies]
# The ONLY brain dependency a sample may have is the SDK facade, and it must
# NAME the surfaces it uses - see samples/README.md rules 1 and 2. `text`
# gets the Qwen3-Embedding backbone this sample exists to demonstrate.
brain = { workspace = true, features = ["text"] }

[package.metadata.brain]
# `make check/samples` fails if the real `cargo tree` closure exceeds this.
# Measured at 32 (`text`'s qwen3 backbone pulls in its full engine stack -
# kernels, gpu-core, model, checkpoint, data - not a lightweight surface;
# `brain-lfm2`, the `EmbeddingPipeline`/`EmbeddingTrainer` LFM2.5-Encoder
# backend, is the same `text`-feature closure's other one crate).
max-brain-crates = 32
