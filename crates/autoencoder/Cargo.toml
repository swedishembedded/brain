[package]
name = "brain-autoencoder"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bottleneck autoencoder (sequence -> single compressed representation -> MLP reconstruction): forward + backprop in WGSL, MSE Regression head."

[lib]
name = "autoencoder"
path = "src/lib.rs"

[dependencies]
brain-gpu-core.workspace = true
brain-model.workspace = true
brain-paramstore.workspace = true
brain-optim.workspace = true
brain-checkpoint.workspace = true
brain-kernels.workspace = true
brain-data.workspace = true
serde_json.workspace = true
bytemuck.workspace = true

[dev-dependencies]
brain-data.workspace = true
brain-model.workspace = true
