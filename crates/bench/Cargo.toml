[package]
name = "brain-bench"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Model-agnostic architecture-evaluation benchmark suite (registry + runner + reference benchmarks like MQAR)."

[lib]
name = "bench"
path = "src/lib.rs"

[dependencies]
brain-data.workspace = true
brain-model.workspace = true
brain-gpt2.workspace = true
brain-toymoe.workspace = true
brain-qwen3.workspace = true
brain-glmdsa.workspace = true
brain-toyautoencoder.workspace = true
brain-eval.workspace = true
brain-checkpoint.workspace = true
# Forecasting scenarios (fcbench) so the forecasting battery registers as
# Benchmarks alongside the LM/algorithmic ones.
brain-fcbench.workspace = true
serde_json.workspace = true
