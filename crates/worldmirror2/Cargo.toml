[package]
name = "brain-worldmirror2"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WorldMirror-2 multi-view 3D reconstruction: DINOv2 ViT-L patch encoder + alternating frame/global attention trunk + DPT/camera/Gaussian heads, imported exactly from the reference checkpoint and run on the brain engine (CPU/GPU/NPU)."

[lib]
name = "worldmirror2"
path = "src/lib.rs"

[dependencies]
brain-gpu-core.workspace = true
brain-kernels.workspace = true
brain-checkpoint.workspace = true
brain-paramstore.workspace = true
brain-vision.workspace = true
brain-imaging.workspace = true
brain-model.workspace = true
brain-splat.workspace = true
serde_json.workspace = true

[dev-dependencies]
# `data::rng::Lcg` — the one deterministic test PRNG (tests only).
brain-data.workspace = true
