[package]
name = "brain-depth"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Monocular depth: ZipDepth (6.1M pure-conv, RepVGG-reparameterizable) behind a dense relative inverse-depth output contract."

[lib]
name = "depth"
path = "src/lib.rs"

[dependencies]
brain-capability.workspace = true
brain-gpu-core.workspace = true
brain-backend-cpu.workspace = true
brain-vision.workspace = true
brain-imaging.workspace = true
brain-model.workspace = true
brain-paramstore.workspace = true
brain-optim.workspace = true
brain-data.workspace = true
brain-checkpoint.workspace = true
brain-kernels.workspace = true
serde_json.workspace = true
bytemuck.workspace = true
# Host-side pre/post-processing (resize + layout pack) runs per camera frame;
# row-parallelising it took it off the frame's critical path.

[dev-dependencies]
brain-gradcheck.workspace = true
