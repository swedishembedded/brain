[package]
name = "brain-sfm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Structure from motion from photographs alone: SIFT features, pair selection by VLAD retrieval, mutual ratio-test matching, five-point LO-RANSAC essential-matrix verification on bearing vectors, global initialization (robust rotation averaging, global positioning) with incremental P3P registration as fallback, triangulation and Levenberg-Marquardt bundle adjustment that self-calibrates each physical camera under a selected lens model (pinhole, Brown-Conrady, Kannala-Brandt fisheye), and georeferencing: metres from GPS by a robust similarity, gravity from how the cameras were held."

[lib]
name = "sfm"
path = "src/lib.rs"

[dependencies]
# `backend_cpu::par` - the workspace's one rayon seam: feature extraction,
# blurs and descriptor matching are data-parallel host loops.
brain-backend-cpu.workspace = true
# `data::rng::Lcg` - the deterministic PRNG, for RANSAC sampling that gives the
# same reconstruction twice.
brain-data.workspace = true
# `camera::Intrinsics` - the workspace's one camera model, which this crate
# calibrates.
brain-camera.workspace = true

[dev-dependencies]
# `imaging::load` - JPEG decoding for the `sfm_folder` example.
brain-imaging.workspace = true
