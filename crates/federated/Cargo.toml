[package]
name = "brain-federated"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sharded/federated MoE: vertical expert shard split/assemble, hash-verified manifests, train-scope, router integration."

[lib]
name = "federated"
path = "src/lib.rs"

[dependencies]
brain-checkpoint.workspace = true
serde_json.workspace = true
